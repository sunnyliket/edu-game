const quizBank = {
    acidBase: [
        {
            question: "붉은색 리트머스 종이는 무엇을 알려 주는 데 쓰이나요?",
            options: ["염기성 용액이면 파란색으로 변하는지 확인한다", "물의 온도를 잰다", "물체의 빠르기를 잰다", "꽃가루의 양을 잰다"],
            correct: 0
        },
        {
            question: "붉은 양배추 지시약에 산성 용액을 넣으면 무슨 색으로 변하나요?",
            options: ["붉은색 또는 분홍색", "노란색", "검은색", "투명한 색"],
            correct: 0
        },
        {
            question: "석회수는 산성 용액인가요, 염기성 용액인가요?",
            options: ["염기성 용액", "산성 용액", "중성 용액", "기체"],
            correct: 0
        },
        {
            question: "산성 용액에 페놀프탈레인 용액을 넣으면 어떤 색으로 변하나요?",
            options: ["색이 거의 변하지 않는다", "진한 분홍색", "초록색", "파란색"],
            correct: 0
        },
        {
            question: "리트머스 종이처럼 용액의 성질에 따라 색깔이 변하는 물질을 무엇이라고 하나요?",
            options: ["지시약", "증산", "속력계", "광합성"],
            correct: 0
        },
        {
            question: "산성 용액에 붉은 양배추 지시약을 넣으면 보통 어떤 색으로 변하나요?",
            options: ["붉은색", "푸른색", "초록색", "갈색"],
            correct: 0
        },
        {
            question: "염기성 용액에 붉은 양배추 지시약을 넣으면 보통 어떤 색으로 변하나요?",
            options: ["푸른색이나 초록색", "붉은색", "흰색", "주황색"],
            correct: 0
        },
        {
            question: "산성 용액은 어떤 물질을 녹일 수 있나요?",
            options: ["금속이나 대리석 같은 물질", "빛", "소리", "바람"],
            correct: 0
        },
        {
            question: "염기성 용액은 어떤 것을 녹이는 성질이 있나요?",
            options: ["단백질이나 기름때", "자석", "그림자", "햇빛"],
            correct: 0
        },
        {
            question: "산성 용액에 염기성 용액을 계속 넣으면 성질이 어떻게 변하나요?",
            options: ["중성에 가까워졌다가 염기성으로 변할 수 있다", "항상 산성만 된다", "항상 얼음이 된다", "색이 절대 변하지 않는다"],
            correct: 0
        },
        {
            question: "염기성 용액에 산성 용액을 계속 넣으면 성질이 어떻게 변하나요?",
            options: ["중성에 가까워졌다가 산성으로 변할 수 있다", "항상 염기성만 된다", "항상 금속이 된다", "항상 기체가 된다"],
            correct: 0
        }
    ],
    motion: [
        {
            question: "시간이 지남에 따라 물체의 위치가 변할 때 물체가 무엇을 한다고 하나요?",
            options: ["운동한다", "증산한다", "공전한다", "수분한다"],
            correct: 0
        },
        {
            question: "속력은 이동 거리를 무엇으로 나누어 구하나요?",
            options: ["걸린 시간", "물체의 색깔", "물체의 모양", "날씨"],
            correct: 0
        },
        {
            question: "버스가 2시간 동안 140km를 이동하면 속력은 얼마인가요?",
            options: ["시속 70km", "시속 140km", "시속 280km", "시속 2km"],
            correct: 0
        },
        {
            question: "교통사고를 줄이기 위한 안전장치는 무엇인가요?",
            options: ["안전벨트", "색연필", "화분", "자석"],
            correct: 0
        },
        {
            question: "과속 방지턱은 무엇을 줄이기 위한 장치인가요?",
            options: ["자동차의 빠른 속도", "햇빛의 양", "물의 높이", "꽃가루의 이동"],
            correct: 0
        },
        {
            question: "안전벨트의 역할로 알맞은 것은 무엇인가요?",
            options: ["몸이 앞으로 튀어나가는 것을 줄인다", "차를 더 빠르게 만든다", "날씨를 바꾼다", "식물이 자라게 한다"],
            correct: 0
        },
        {
            question: "운동하는 물체의 예로 알맞은 것은 무엇인가요?",
            options: ["도로를 달리는 자동차", "책상 위에 가만히 놓인 책", "벽에 붙은 시계 그림", "닫혀 있는 상자"],
            correct: 0
        },
        {
            question: "버스는 4시간에 240km, 기차는 6시간에 600km를 갔습니다. 어느 것이 더 빠른가요?",
            options: ["기차", "버스", "둘 다 움직이지 않는다", "비교할 수 없다"],
            correct: 0
        },
        {
            question: "교통 안전을 위해 사용할 수 있는 안전장치 하나는 무엇인가요?",
            options: ["헬멧", "꽃병", "나뭇잎", "물컵"],
            correct: 0
        }
    ],
    plant: [
        {
            question: "잎에 도달한 물이 수증기가 되어 밖으로 나가는 작용은 무엇인가요?",
            options: ["증산작용", "뿌리털", "암술", "씨앗"],
            correct: 0
        },
        {
            question: "뿌리의 기능 하나로 알맞은 것은 무엇인가요?",
            options: ["물과 무기 양분을 흡수한다", "햇빛을 만든다", "꽃가루를 날린다", "낮과 밤을 만든다"],
            correct: 0
        },
        {
            question: "꽃가루가 암술로 옮겨지는 과정을 무엇이라고 하나요?",
            options: ["수분", "증산", "자전", "속력"],
            correct: 0
        },
        {
            question: "식물이 햇빛을 이용해 스스로 양분을 만드는 과정을 무엇이라고 하나요?",
            options: ["광합성", "공전", "중화", "운동"],
            correct: 0
        },
        {
            question: "뿌리는 흙 속에서 주로 무엇을 흡수하나요?",
            options: ["물과 무기 양분", "달빛", "꽃가루", "소리"],
            correct: 0
        },
        {
            question: "생물을 이루는 기본 단위는 무엇인가요?",
            options: ["세포", "행성", "속력", "지시약"],
            correct: 0
        },
        {
            question: "봉선화 줄기의 특징으로 알맞은 것은 무엇인가요?",
            options: ["곧게 서고 물과 양분이 이동하는 통로가 있다", "땅속에서만 자란다", "항상 돌처럼 단단하다", "꽃가루만 만든다"],
            correct: 0
        },
        {
            question: "증산작용은 무엇을 말하나요?",
            options: ["식물 속 물이 수증기가 되어 밖으로 나가는 작용", "지구가 태양 주위를 도는 운동", "자동차가 멈추는 일", "산과 염기가 만나는 일"],
            correct: 0
        },
        {
            question: "뿌리의 역할로 알맞은 것은 무엇인가요?",
            options: ["식물을 지탱하고 물을 흡수한다", "낮과 밤을 만든다", "용액의 색을 바꾼다", "속력을 계산한다"],
            correct: 0
        },
        {
            question: "줄기의 역할로 알맞은 것은 무엇인가요?",
            options: ["물과 양분이 이동하는 길이 된다", "태양 주위를 돈다", "안전벨트를 만든다", "리트머스 종이를 파랗게 한다"],
            correct: 0
        }
    ],
    earth: [
        {
            question: "지구는 무엇을 기준으로 회전하나요?",
            options: ["지축", "꽃잎", "강물", "안전벨트"],
            correct: 0
        },
        {
            question: "지구는 어느 방향으로 자전하나요?",
            options: ["서쪽에서 동쪽으로", "동쪽에서 서쪽으로", "북쪽에서 남쪽으로", "아래에서 위로"],
            correct: 0
        },
        {
            question: "지구는 하루에 몇 바퀴 자전하나요?",
            options: ["한 바퀴", "두 바퀴", "열 바퀴", "반 바퀴"],
            correct: 0
        },
        {
            question: "낮과 밤은 하루에 몇 번씩 오나요?",
            options: ["낮 한 번, 밤 한 번", "낮만 세 번", "밤만 네 번", "전혀 오지 않는다"],
            correct: 0
        },
        {
            question: "지구는 태양을 중심으로 무엇을 하나요?",
            options: ["공전", "수분", "증산", "중화"],
            correct: 0
        },
        {
            question: "지구가 태양 주위를 공전하는 데 걸리는 시간은 얼마인가요?",
            options: ["약 1년", "약 하루", "약 1시간", "약 1분"],
            correct: 0
        },
        {
            question: "낮과 밤이 생기는 까닭은 무엇인가요?",
            options: ["지구가 자전하기 때문", "식물이 물을 흡수하기 때문", "버스가 움직이기 때문", "리트머스 종이가 있기 때문"],
            correct: 0
        },
        {
            question: "공전이란 무엇인가요?",
            options: ["지구가 태양 주위를 도는 운동", "지구가 제자리에서 도는 운동", "물이 수증기로 나가는 작용", "꽃가루가 암술로 옮겨지는 과정"],
            correct: 0
        },
        {
            question: "지구의 한쪽이 태양 빛을 받지 못하면 그곳은 밤인가요, 낮인가요?",
            options: ["밤", "낮", "항상 저녁", "항상 아침"],
            correct: 0
        },
        {
            question: "태양은 하루 동안 어디에서 어디로 이동하는 것처럼 보이나요?",
            options: ["동쪽에서 서쪽으로", "서쪽에서 동쪽으로", "북쪽에서 남쪽으로", "땅속에서 하늘로"],
            correct: 0
        }
    ]
};

const allQuestions = [
    ...quizBank.acidBase,
    ...quizBank.motion,
    ...quizBank.plant,
    ...quizBank.earth
];

const maps = {
    polluted: [
        "TTTGGGGGGGGGGGGGGGTTT",
        "TGGGGFGGGGGRGGGGGGGGT",
        "TGGPPPPPPPPPPPPPPPGGT",
        "TGGPWWGGGGCCCGGGPPPGT",
        "TGGPWWGGGCCCCCGRGGGGT",
        "TGGPPPGGGGCCCGGGGGGGT",
        "TGGGGRGGGGGGGGGGFGGGT",
        "TGPPPPPPPPPPPPPPPPPGT",
        "TGPWWWGGGGGRGGGCCGGGT",
        "TGPWGGGGFGGGGGGCCGGGT",
        "TGPPPPPPPPPPPPPPGGGGT",
        "TGGGGGGGGRGGGGGGGGGGT",
        "TGGGFGGGGGGGFGGGGGGGT",
        "TTTGGGGGGGGGGGGGGGTTT"
    ],
    halfClean: [
        "TTTGGGFGGGGGGGGGFGTTT",
        "TGGGGRGGFFGGGGGGGGGGT",
        "TGFGPPPPPPPPPPPPPPGGT",
        "TGGPWWGGGGGGGGGGPPPGT",
        "TGGPWWGGGFGGGGCRGGGGT",
        "TGGPPPGGGGGGGGGGGGGGT",
        "TGGGGRGGGGFFGGGGFGGGT",
        "TGPPPPPPPPPPPPPPPPPGT",
        "TGPWWWGGGGGRGGGGGGGGT",
        "TGPWGGFGGGGGGGGGGGGGT",
        "TGPPPPPPPPPPPPPPGGGGT",
        "TGGGGGGGGRGGGGGGGGGGT",
        "TGGGFGGGGGGGFGGGGGGGT",
        "TTTGGGGGGGGGGGGGGGTTT"
    ],
    forest: [
        "TTTGGGFGGGGGGGGGFGTTT",
        "TGFGGRGGGFFGGGFGGGGGT",
        "TGGFPPPPPPPPPPPPPPGGT",
        "TGGPWWGGFGGFFGGGPPPGT",
        "TGGPWWGFGGGFGGCRGGGGT",
        "TGGPPPGGGFGGGGGGGGGGT",
        "TGGFGRGGGGFFGGGGFGGGT",
        "TGPPPPPPPPPPPPPPPPPGT",
        "TGPWWWGGGGGRGGGFGGGGT",
        "TGPWGFGGGGGGGGFGGGGGT",
        "TGPPPPPPPPPPPPPPGGGGT",
        "TGGGFGGGGRGGGGGFGGGGT",
        "TGGGFGGGGGGGFGGGGGGGT",
        "TTTGGGGGGGGGGGGGGGTTT"
    ],
    sky: [
        "TTTGGGGGGGGGGGGGGGTTT",
        "TGGGGRGGGGFGGGGGGGGGT",
        "TGGPPPPPPPPPPPPPPPGGT",
        "TGGPWWGGGGCCGGGGPPPGT",
        "TGGPWWGGGGCRGGGRGGGGT",
        "TGGPPPGGGGGGGGGGGGGGT",
        "TGGGGRGGGFRGGGGGFGGGT",
        "TGPPPPPPPPPPPPPPPPPGT",
        "TGPWWWGGGGGRGGGGGGGGT",
        "TGPWGGGGFGGGGGGGGGGGT",
        "TGPPPPPPPPPPPPPPGGGGT",
        "TGGGGGGGGRGGGGGGGGGGT",
        "TGGGFGGGGGGGFGGGGGGGT",
        "TTTGGGGGGGGGGGGGGGTTT"
    ],
    boss: [
        "TTTGGGGGGGGGGGGGGGTTT",
        "TGGGGRGGGGGGGGGGGGGGT",
        "TGGPPPPPPPPPPPPPPPGGT",
        "TGGDDDDGGGGDDDDGPPPGT",
        "TGPDDDGGGGDDDDDRGGGGT",
        "TGGPPPGGGGGDDGGGGGGGT",
        "TGGGGRGGGGDDGGGGGGGGT",
        "TGPPPPPPPPPPPPPPPPPGT",
        "TGPWWWGGGGGRGGGDDGGGT",
        "TGPWGGGGGGGGGGGDDGGGT",
        "TGPPPPPPPPPPPPPPGGGGT",
        "TGGGGGGGGRGGGGGGGGGGT",
        "TGGGGGGGGGGGGGGGGGGGT",
        "TTTGGGGGGGGGGGGGGGTTT"
    ],
    ending: [
        "TTTGGGFGGGGGGGGGFGTTT",
        "TGFGGFGGGFFGGGFFGGGGT",
        "TGGFPPPPPPPPPPPPPPGGT",
        "TGGPWWGGFGGFFGGGPPPGT",
        "TGPGFGGGFGFRGGFGGGGGT",
        "TGGPPPGGGGFGGGGGGGGGT",
        "TGPWGFGGGGGFGGGGFGGGT",
        "TGPPPPPPPPPPPPPPPPPGT",
        "TGPWWWGGFGGRGGGGGGGGT",
        "TGPWGFGGGGGGGGFGGGGGT",
        "TGPPPPPPPPPPPPPPGGGGT",
        "TGGGFGGFGFGGGGGGGGGGT",
        "TGGGFGGGGGGGFGGGGGGGT",
        "TTTGGGGGGGGGGGGGGGTTT"
    ]
};

const WIDE_MAP_WIDTH = 96;
const WIDE_MAP_HEIGHT = 64;

function expandAdventureMap(baseMap, variant = 0) {
    const baseHeight = baseMap.length;
    const baseWidth = baseMap[0]?.length || 0;
    const rows = [];

    for (let y = 0; y < WIDE_MAP_HEIGHT; y += 1) {
        let row = "";
        for (let x = 0; x < WIDE_MAP_WIDTH; x += 1) {
            let tile = "G";
            const isOuterWall = x === 0 || y === 0 || x === WIDE_MAP_WIDTH - 1 || y === WIDE_MAP_HEIGHT - 1;

            if (isOuterWall) {
                tile = "T";
            } else if (y < baseHeight && x < baseWidth) {
                tile = baseMap[y][x] || "G";
                if (x === baseWidth - 1 && [2, 7, 10, 12].includes(y)) tile = "P";
                if (y === baseHeight - 1 && [5, 10, 15, 18].includes(x)) tile = "P";
            } else {
                const mainRoad = y === 7 || x === 10 || y === 18 || x === 28 || y === 36 || x === 54 || y === 52 || x === 78;
                const sideRoad = (x > 20 && y === 3) || (y > 13 && x === 18) || (x > 26 && y === 23) ||
                    (x > 44 && x < 88 && y === 12) || (y > 30 && y < 58 && x === 40) || (x > 58 && y === 45);
                const river = ((x >= 24 && x <= 26) || (x >= 66 && x <= 69)) && ![7, 18, 23, 36, 45, 52].includes(y);
                const lake = x > 70 && x < 86 && y > 24 && y < 34 && (x + y + variant) % 3 !== 0;
                const cave = (x > 31 && y > 3 && y < 11) || (x > 80 && y > 44 && y < 58);
                const grove = x > 44 && x < 70 && y > 4 && y < 22;
                const deepForest = x > 18 && x < 46 && y > 38 && y < 60;
                const forest = ((x * 5 + y * 7 + variant) % (grove || deepForest ? 7 : 13) === 0);
                const rock = (x * 3 + y * 11 + variant) % (cave ? 11 : 29) === 0;
                const flower = (x * 7 + y * 3 + variant) % 31 === 0;
                const ruin = x > 58 && x < 72 && y > 42 && y < 52 && (x + y + variant) % 4 === 0;

                if (mainRoad || sideRoad) tile = "P";
                else if (river || lake) tile = "W";
                else if (cave) tile = (x + y + variant) % 5 === 0 ? "R" : "D";
                else if (ruin) tile = (x + y) % 2 === 0 ? "R" : "D";
                else if (forest) tile = "T";
                else if (rock) tile = "R";
                else if (flower) tile = "F";
            }

            row += tile;
        }
        rows.push(row);
    }

    return rows;
}

Object.keys(maps).forEach((key, index) => {
    maps[key] = expandAdventureMap(maps[key], index);
});

const stageThemes = {
    1: {
        pageBackground: "linear-gradient(180deg, #6ea7c9 0%, #587b68 54%, #2d4a32 100%)",
        skyTop: "#78b9d7",
        skyMid: "#a9c98a",
        horizon: "#5f914f",
        ground: "#274f2f",
        vignette: 0.2,
        accent: "#8cff7a",
        music: {
            interval: 620,
            wave: "triangle",
            volume: 0.034,
            bassVolume: 0.018,
            notes: [311.13, 392.0, 466.16, 392.0, 349.23, 392.0, 466.16, 523.25],
            bass: [155.56, 174.61, 146.83, 130.81]
        }
    },
    2: {
        pageBackground: "linear-gradient(180deg, #4a1a2e 0%, #6b2020 48%, #1a0a0a 100%)",
        skyTop: "#1a0508",
        skyMid: "#6b1a1a",
        horizon: "#8b2a1a",
        ground: "#1a0a05",
        vignette: 0.38,
        accent: "#ff6b6b",
        bloodMoon: true,
        music: {
            interval: 540,
            wave: "sine",
            volume: 0.032,
            bassVolume: 0.02,
            notes: [293.66, 349.23, 440.0, 392.0, 329.63, 440.0, 493.88, 392.0],
            bass: [146.83, 146.83, 164.81, 130.81]
        }
    },
    3: {
        pageBackground: "linear-gradient(180deg, #3f6f72 0%, #3d6741 46%, #162c21 100%)",
        skyTop: "#416e79",
        skyMid: "#5f8756",
        horizon: "#35683b",
        ground: "#172e22",
        vignette: 0.34,
        accent: "#65e879",
        music: {
            interval: 700,
            wave: "triangle",
            volume: 0.03,
            bassVolume: 0.022,
            notes: [261.63, 329.63, 392.0, 349.23, 293.66, 392.0, 440.0, 349.23],
            bass: [130.81, 98.0, 116.54, 87.31]
        }
    },
    4: {
        pageBackground: "linear-gradient(180deg, #18264b 0%, #27335f 44%, #111926 100%)",
        skyTop: "#17254b",
        skyMid: "#34406b",
        horizon: "#272d43",
        ground: "#121a27",
        vignette: 0.42,
        accent: "#b9c7ff",
        music: {
            interval: 760,
            wave: "sine",
            volume: 0.028,
            bassVolume: 0.026,
            notes: [246.94, 329.63, 415.3, 369.99, 277.18, 369.99, 493.88, 415.3],
            bass: [92.5, 123.47, 103.83, 82.41]
        }
    },
    5: {
        pageBackground: "linear-gradient(180deg, #210f1c 0%, #3d1822 42%, #08080c 100%)",
        skyTop: "#1b0d18",
        skyMid: "#4a1d23",
        horizon: "#6a2b22",
        ground: "#0a090d",
        vignette: 0.56,
        accent: "#ff6a55",
        music: {
            interval: 820,
            wave: "sawtooth",
            volume: 0.025,
            bassVolume: 0.035,
            notes: [196.0, 261.63, 311.13, 293.66, 220.0, 293.66, 349.23, 311.13],
            bass: [65.41, 73.42, 87.31, 61.74]
        }
    },
    6: {
        pageBackground: "linear-gradient(180deg, #466f8f 0%, #6c8f6c 44%, #243a2b 100%)",
        skyTop: "#5f92b2",
        skyMid: "#b4b67b",
        horizon: "#6f9b5d",
        ground: "#213f2d",
        vignette: 0.24,
        accent: "#ffd66e",
        music: {
            interval: 680,
            wave: "triangle",
            volume: 0.032,
            bassVolume: 0.02,
            notes: [329.63, 392.0, 493.88, 440.0, 392.0, 523.25, 587.33, 493.88],
            bass: [164.81, 196.0, 146.83, 174.61]
        }
    }
};

const game = {
    storageKey: "science-superpower-adventure-save",
    rows: WIDE_MAP_HEIGHT,
    cols: WIDE_MAP_WIDTH,
    stageIndex: 0,
    score: 0,
    emeralds: 0,
    totalAnswers: 0,
    correctAnswers: 0,
    completedStages: [],
    earnedPowers: [],
    stageWorldState: {},
    player: { x: 2, y: 11 },
    currentScreen: "startScreen",
    nearbyEntity: null,
    selectedAnswer: null,
    answerLocked: false,
    dungeon: null,
    timerId: null,
    isTransitioning: false,
    pendingDialogueCallback: null,
    pendingRewardCallback: null,
    promptTimer: null,
    stageTitleTimer: null,

    stages: [
        {
            id: 1,
            name: "산과 염기",
            map: maps.polluted,
            start: { x: 2, y: 11 },
            questions: quizBank.acidBase,
            power: "산염기 정화 초능력",
            rewardTitle: "과학실 실험용 도구 카드 획득!",
            rewardText: "카드에서 빛이 나며 산염기 정화 초능력이 깨어났어요. 이 힘으로 슬라임 괴물을 물리칠 수 있어요.",
            rewardEmeralds: 5,
            clearBonus: 100,
            npc: {
                name: "슬라임",
                label: "슬라임",
                portrait: "villager",
                className: "entity-npc",
                x: 3,
                y: 7,
                dialogue: "과학 초능력이 흩어졌어!\n산성 몬스터와 염기성 몬스터가\n초능력을 가져가기 전에 우리가 먼저 찾아야 해!!\n같이 찾아보자!"
            },
            monster: {
                name: "산성 몬스터",
                label: "산성",
                portrait: "monster",
                className: "entity-monster entity-acid",
                x: 8,
                y: 3,
                dialogue: "하하! 퀴즈를 맞히면 문제 5개가 기다린다. 맞춰 봐라!\n과학실 실험용 도구 카드가 있으면 나도 물러날 수밖에 없지."
            },
            portal: { label: "포탈", x: 18, y: 3 }
        },
        {
            id: 2,
            name: "물체의 운동",
            map: maps.halfClean,
            start: { x: 2, y: 11 },
            questions: quizBank.motion,
            power: "속력 질주 초능력",
            rewardTitle: "속력 질주 초능력 획득!",
            rewardText: "길이 더 밝아지고 주민 한 명이 마을로 돌아왔어요.",
            rewardEmeralds: 6,
            clearBonus: 120,
            npc: {
                name: "주민",
                label: "주민",
                portrait: "villager",
                className: "entity-npc",
                x: 3,
                y: 7,
                dialogue: "스테이지 2를 클리어 하러 가자!"
            },
            monster: {
                name: "빠른 슬라임",
                label: "슬라임",
                portrait: "monster",
                className: "entity-monster",
                x: 8,
                y: 3,
                dialogue: "이 길은 속력을 모르면 지나가기 어려워!\n운동 문제를 풀 준비가 되면 포탈로 가자."
            },
            portal: { label: "포탈", x: 18, y: 3 }
        },
        {
            id: 3,
            name: "식물의 구조와 기능",
            map: maps.forest,
            start: { x: 2, y: 11 },
            questions: quizBank.plant,
            power: "식물 성장 초능력",
            rewardTitle: "식물 성장 초능력 획득!",
            rewardText: "꽃이 피어나고 오염된 땅이 풀밭으로 바뀌었어요.",
            rewardEmeralds: 7,
            clearBonus: 140,
            npc: {
                name: "숲 주민",
                label: "주민",
                portrait: "villager",
                className: "entity-npc",
                x: 3,
                y: 7,
                dialogue: "이번에도 스테이지 3을 클리어 하자!!"
            },
            monster: {
                name: "덩굴 몬스터",
                label: "덩굴",
                portrait: "monster",
                className: "entity-monster",
                x: 8,
                y: 3,
                dialogue: "식물의 구조를 알면 덩굴 길이 열릴 거야.\n포탈 던전에서 초능력을 찾아와!"
            },
            portal: { label: "포탈", x: 18, y: 3 }
        },
        {
            id: 4,
            name: "지구의 운동",
            map: maps.sky,
            start: { x: 2, y: 11 },
            questions: quizBank.earth,
            power: "지구 회전 초능력",
            rewardTitle: "지구 회전 초능력 획득!",
            rewardText: "하늘이 맑아지고 마을 시계탑이 다시 움직이기 시작했어요.",
            rewardEmeralds: 8,
            clearBonus: 160,
            npc: {
                name: "별빛 주민",
                label: "주민",
                portrait: "villager",
                className: "entity-npc",
                x: 3,
                y: 7,
                dialogue: "과학 초능력을 얻으러 가자!"
            },
            monster: {
                name: "그림자 몬스터",
                label: "그림자",
                portrait: "monster",
                className: "entity-monster entity-base",
                x: 8,
                y: 3,
                dialogue: "낮과 밤의 비밀을 모르면 지나갈 수 없어!\n지구의 운동 문제를 풀어 봐."
            },
            portal: { label: "포탈", x: 18, y: 3 }
        },
        {
            id: 5,
            name: "중간보스전",
            map: maps.boss,
            start: { x: 2, y: 11 },
            boss: true,
            randomCount: 5,
            timeLimit: 60,
            power: "골렘 방패 초능력",
            rewardTitle: "복구 완료!",
            rewardText: "중간보스가 물러나고 마을 중심이 복구되었어요.",
            rewardEmeralds: 10,
            clearBonus: 220,
            npc: {
                name: "철골렘",
                label: "철골렘",
                portrait: "golem",
                className: "entity-golem",
                x: 3,
                y: 7,
                dialogue: "스테이지 5를 깨자!"
            },
            monster: {
                name: "오염 골렘",
                label: "보스",
                portrait: "monster",
                className: "entity-monster entity-boss",
                x: 8,
                y: 3,
                dialogue: "랜덤 문제 5개를 제한시간 안에 맞혀야 길이 열린다!\n내 체력을 모두 줄여 봐!"
            },
            portal: { label: "보스포탈", x: 18, y: 3 }
        },
        {
            id: 6,
            name: "휴식과 엔딩 공간",
            map: maps.ending,
            start: { x: 2, y: 11 },
            restReview: true,
            randomCount: 3,
            power: "휴식 회복 초능력",
            rewardTitle: "마지막 보상 획득!",
            rewardText: "고기 만찬을 나누며 마을 사람들이 모두 쉬고 있어요.",
            rewardEmeralds: 12,
            clearBonus: 260,
            npc: {
                name: "주민",
                label: "주민",
                portrait: "villager",
                className: "entity-npc",
                x: 3,
                y: 7,
                dialogue: "와 축하해!"
            },
            monster: {
                name: "정화된 슬라임",
                label: "친구",
                portrait: "monster",
                className: "entity-monster",
                x: 8,
                y: 7,
                dialogue: "이제 마을을 막지 않을게!\n마지막 휴식 포탈에서 배운 내용을 확인하자."
            },
            portal: { label: "휴식포탈", x: 18, y: 10 },
            decorations: [
                { label: "고기", className: "entity-feast", x: 6, y: 7 },
                { label: "주민", className: "entity-resident", x: 11, y: 7 }
            ]
        }
    ],

    init() {
        this.cacheElements();
        this.bindEvents();
        this.resetRuntimeState();
        this.elements.continueButton.hidden = !this.hasSavedGame();
        this.updateHud();
        this.setupBackgrounds();
        this.showScreen(this.currentScreen);
    },

    cacheElements() {
        this.elements = {
            screens: document.querySelectorAll(".screen"),
            stageInfo: document.getElementById("stage-info"),
            score: document.getElementById("score"),
            villageStatus: document.getElementById("villageStatus"),
            cleanMeterFill: document.getElementById("cleanMeterFill"),
            gameBackground: document.getElementById("gameBackground"),
            worldMap: document.getElementById("worldMap"),
            questText: document.getElementById("questText"),
            worldStageTitle: document.getElementById("worldStageTitle"),
            interactPrompt: document.getElementById("interactPrompt"),
            continueButton: document.getElementById("continueButton"),
            resetButton: document.getElementById("resetButton"),
            dialogueOverlay: document.getElementById("dialogueOverlay"),
            dialoguePortrait: document.getElementById("dialoguePortrait"),
            dialogueSpeaker: document.getElementById("dialogueSpeaker"),
            dialogueText: document.getElementById("dialogueText"),
            dialogueNextButton: document.getElementById("dialogueNextButton"),
            stageTitleOverlay: document.getElementById("stageTitleOverlay"),
            stageTitleNumber: document.getElementById("stageTitleNumber"),
            stageTitleName: document.getElementById("stageTitleName"),
            portalTransition: document.getElementById("portalTransition"),
            dungeonTitle: document.getElementById("dungeonTitle"),
            questionCounter: document.getElementById("questionCounter"),
            quizRewardHint: document.getElementById("quizRewardHint"),
            quizProgress: document.getElementById("quizProgress"),
            questionText: document.getElementById("questionText"),
            answerOptions: document.getElementById("answerOptions"),
            quizFeedback: document.getElementById("quizFeedback"),
            quizCard: document.getElementById("quizCard"),
            submitAnswerButton: document.getElementById("submitAnswerButton"),
            timerPanel: document.getElementById("timerPanel"),
            timerText: document.getElementById("timerText"),
            accuracyText: document.getElementById("accuracyText"),
            bossPanel: document.getElementById("bossPanel"),
            bossName: document.getElementById("bossName"),
            bossHpText: document.getElementById("bossHpText"),
            bossHpFill: document.getElementById("bossHpFill"),
            rewardModal: document.getElementById("rewardModal"),
            rewardTitle: document.getElementById("rewardTitle"),
            rewardText: document.getElementById("rewardText"),
            rewardButton: document.getElementById("rewardButton"),
            emeraldCount: document.getElementById("emeraldCount"),
            finalScore: document.getElementById("finalScore"),
            finalAccuracy: document.getElementById("finalAccuracy"),
            finalEmeralds: document.getElementById("finalEmeralds"),
            finalPowers: document.getElementById("finalPowers")
        };
    },

    bindEvents() {
        document.addEventListener("keydown", (event) => this.handleKeydown(event));
        this.elements.submitAnswerButton.addEventListener("click", () => this.submitAnswer());
        this.elements.dialogueNextButton.addEventListener("click", () => this.closeDialogue());
        this.elements.rewardButton.addEventListener("click", () => this.closeReward());
        this.elements.resetButton.addEventListener("click", () => this.restart());

        document.querySelectorAll("[data-move]").forEach((button) => {
            button.addEventListener("click", () => this.movePlayer(button.dataset.move));
        });
    },

    resetRuntimeState() {
        this.stageIndex = 0;
        this.score = 0;
        this.emeralds = 0;
        this.totalAnswers = 0;
        this.correctAnswers = 0;
        this.completedStages = [];
        this.earnedPowers = [];
        this.stageWorldState = {};
        this.player = { x: 2, y: 7 };
        this.dungeon = null;
        this.selectedAnswer = null;
        this.answerLocked = false;
        this.nearbyEntity = null;
        this.isTransitioning = false;
        this.clearTimer();
    },

    startGame() {
        localStorage.removeItem(this.storageKey);
        this.resetRuntimeState();
        this.enterStage(0);
    },

    continueGame() {
        const savedGame = this.loadSavedGame();
        if (!savedGame) {
            this.startGame();
            return;
        }

        this.resetRuntimeState();
        this.applySavedGame(savedGame);

        if (savedGame.finished) {
            this.completeGame();
            return;
        }

        this.enterStage(this.stageIndex, true);
    },

    restart() {
        localStorage.removeItem(this.storageKey);
        this.resetRuntimeState();
        this.elements.continueButton.hidden = true;
        this.showScreen("startScreen");
        this.updateHud();
        this.setupBackgrounds();
    },

    enterStage(index, resumed = false) {
        this.clearTimer();
        this.stageIndex = Math.min(index, this.stages.length - 1);
        const stage = this.getCurrentStage();
        this.ensureStageState(stage.id);
        this.player = { ...stage.start };
        this.dungeon = null;
        this.selectedAnswer = null;
        this.answerLocked = false;

        this.showScreen("worldScreen");
        this.renderWorld();
        this.updateHud();
        this.updateQuest();
        this.showStageTitle(stage);
        this.saveProgress();

        if (stage.id === 1 && !this.getStageState().introShown && !resumed) {
            this.getStageState().introShown = true;
            setTimeout(() => {
                if (this.currentScreen === "worldScreen") {
                    this.showDialogue(stage.npc.name, stage.npc.dialogue, stage.npc.portrait);
                }
            }, 1550);
        }
    },

    getCurrentStage() {
        return this.stages[this.stageIndex];
    },

    ensureStageState(stageId) {
        if (!this.stageWorldState[stageId]) {
            this.stageWorldState[stageId] = {
                npcTalked: false,
                monsterMet: false,
                introShown: false
            };
        }
    },

    getStageState() {
        const stage = this.getCurrentStage();
        this.ensureStageState(stage.id);
        return this.stageWorldState[stage.id];
    },

    renderWorld() {
        const stage = this.getCurrentStage();
        const map = stage.map;
        const worldMap = this.elements.worldMap;
        worldMap.innerHTML = "";
        worldMap.style.setProperty("--cols", this.cols);
        worldMap.style.setProperty("--rows", this.rows);

        for (let y = 0; y < this.rows; y += 1) {
            for (let x = 0; x < this.cols; x += 1) {
                const tile = document.createElement("div");
                tile.className = `tile ${this.getTileClass(map[y]?.[x] || "G", x, y)}`;
                worldMap.appendChild(tile);
            }
        }

        this.getVisibleEntities().forEach((entity) => {
            const entityElement = document.createElement("div");
            entityElement.className = `map-sprite entity ${entity.className}`;
            entityElement.dataset.entityId = entity.id || entity.kind || entity.label;
            entityElement.style.setProperty("--x", entity.x);
            entityElement.style.setProperty("--y", entity.y);

            const label = document.createElement("span");
            label.className = "entity-label";
            label.textContent = entity.label;
            entityElement.appendChild(label);
            worldMap.appendChild(entityElement);
        });

        const playerElement = document.createElement("div");
        playerElement.className = "map-sprite player";
        worldMap.appendChild(playerElement);
        this.playerElement = playerElement;
        this.renderPlayerPosition();
        this.updateInteractPrompt();
    },

    getVisibleEntities() {
        const stage = this.getCurrentStage();
        const cleanLevel = this.completedStages.length;
        const entities = [
            { ...stage.npc, id: "npc", kind: "npc" },
            { ...stage.monster, id: "monster", kind: "monster" },
            { ...stage.portal, id: "portal", kind: "portal", className: "entity-portal" }
        ];

        if (stage.decorations) {
            stage.decorations.forEach((decoration, index) => {
                entities.push({ ...decoration, id: `decoration-${index}`, kind: "decoration" });
            });
        }

        const returningResidents = [
            { label: "주민", className: "entity-resident", x: 11, y: 7 },
            { label: "주민", className: "entity-resident", x: 4, y: 2 },
            { label: "주민", className: "entity-resident", x: 10, y: 2 },
            { label: "꽃밭", className: "entity-resident", x: 12, y: 5 }
        ];

        returningResidents.slice(0, Math.min(cleanLevel, returningResidents.length)).forEach((resident, index) => {
            entities.push({ ...resident, id: `resident-${index}`, kind: "decoration" });
        });

        return entities;
    },

    getTileClass(tileType, x, y) {
        const cleanLevel = this.completedStages.length;

        if (tileType === "C" && cleanLevel > ((x + y) % 6)) {
            return "tile-flower";
        }

        if (tileType === "G" && cleanLevel >= 3 && (x * 2 + y) % 11 === 0) {
            return "tile-flower";
        }

        const tileClasses = {
            G: "tile-grass",
            P: "tile-path",
            T: "tile-tree",
            R: "tile-rock",
            W: "tile-water",
            C: "tile-pollution",
            F: "tile-flower",
            D: "tile-dungeon"
        };

        return tileClasses[tileType] || "tile-grass";
    },

    renderPlayerPosition() {
        if (!this.playerElement) return;
        this.playerElement.style.setProperty("--x", this.player.x);
        this.playerElement.style.setProperty("--y", this.player.y);
    },

    handleKeydown(event) {
        const key = event.key.toLowerCase();

        if (!this.elements.dialogueOverlay.hidden && ["q", "enter", " "].includes(key)) {
            event.preventDefault();
            this.closeDialogue();
            return;
        }

        if (!this.elements.rewardModal.hidden && ["enter", " "].includes(key)) {
            event.preventDefault();
            this.closeReward();
            return;
        }

        if (this.currentScreen === "dungeonScreen" && key === "enter") {
            event.preventDefault();
            this.submitAnswer();
            return;
        }

        if (this.currentScreen !== "worldScreen" || this.isOverlayOpen()) {
            return;
        }

        const movementKeys = {
            arrowup: "up",
            w: "up",
            arrowdown: "down",
            s: "down",
            arrowleft: "left",
            a: "left",
            arrowright: "right",
            d: "right"
        };

        if (movementKeys[key]) {
            event.preventDefault();
            this.movePlayer(movementKeys[key]);
        } else if (key === "e") {
            event.preventDefault();
            this.interact();
        }
    },

    movePlayer(direction) {
        if (this.currentScreen !== "worldScreen" || this.isOverlayOpen() || this.isTransitioning) return;

        const moves = {
            up: { x: 0, y: -1 },
            down: { x: 0, y: 1 },
            left: { x: -1, y: 0 },
            right: { x: 1, y: 0 }
        };
        const delta = moves[direction];
        if (!delta) return;

        const nextPosition = {
            x: this.player.x + delta.x,
            y: this.player.y + delta.y
        };

        if (this.isBlocked(nextPosition.x, nextPosition.y)) {
            this.showTemporaryPrompt("길이 막혀 있어요. 주민/장치는 Q, 나무와 돌은 E 키를 눌러 보세요.");
            return;
        }

        this.player = nextPosition;
        this.renderPlayerPosition();
        this.updateInteractPrompt();
        this.checkPortalEntry();
        this.saveProgress();
    },

    isBlocked(x, y) {
        if (x < 0 || y < 0 || x >= this.cols || y >= this.rows) return true;

        const stage = this.getCurrentStage();
        const tileType = stage.map[y]?.[x] || "G";
        if (["T", "R", "W"].includes(tileType)) return true;

        const stageState = this.getStageState();
        const monster = stage.monster;
        if (!stageState.monsterMet && monster.x === x && monster.y === y) return true;

        const blockingDecoration = (stage.decorations || []).some((decoration) => (
            decoration.x === x && decoration.y === y
        ));
        return blockingDecoration;
    },

    updateInteractPrompt() {
        const entity = this.getNearbyInteractiveEntity();
        const mineTarget = this.getMineTarget();
        this.nearbyEntity = entity;

        if (!entity && !mineTarget) {
            this.elements.interactPrompt.hidden = true;
            return;
        }

        this.elements.interactPrompt.textContent = `Q 키를 눌러 ${entity.name}와 대화`;
        this.elements.interactPrompt.hidden = false;
    },

    getNearbyInteractiveEntity() {
        const stage = this.getCurrentStage();
        const candidates = [
            { ...stage.npc, kind: "npc" },
            { ...stage.monster, kind: "monster" }
        ];

        return candidates.find((entity) => {
            const distance = Math.abs(entity.x - this.player.x) + Math.abs(entity.y - this.player.y);
            return distance <= 1;
        }) || null;
    },

    interact() {
        const entity = this.nearbyEntity || this.getNearbyInteractiveEntity();
        if (!entity) {
            this.showTemporaryPrompt("가까운 주민이나 몬스터가 없어요.");
            return;
        }

        const stageState = this.getStageState();
        if (entity.kind === "npc") {
            stageState.npcTalked = true;
        }
        if (entity.kind === "monster") {
            stageState.monsterMet = true;
        }

        this.updateQuest();
        this.renderWorld();
        this.saveProgress();
        this.showDialogue(entity.name, entity.dialogue, entity.portrait);
    },

    checkPortalEntry() {
        const stage = this.getCurrentStage();
        const stageState = this.getStageState();
        const isOnPortal = this.player.x === stage.portal.x && this.player.y === stage.portal.y;
        if (!isOnPortal || this.isTransitioning) return;

        if (!stageState.npcTalked) {
            this.showTemporaryPrompt("먼저 주민과 대화해야 포탈이 열려요.");
            return;
        }

        if (!stageState.monsterMet) {
            this.showTemporaryPrompt("몬스터 지역을 확인해야 포탈이 열려요.");
            return;
        }

        this.enterPortal();
    },

    enterPortal() {
        this.isTransitioning = true;
        this.elements.portalTransition.hidden = false;

        setTimeout(() => {
            this.elements.portalTransition.hidden = true;
            this.isTransitioning = false;
            this.startDungeon();
        }, 950);
    },

    startDungeon() {
        const stage = this.getCurrentStage();
        let questions = stage.questions || [];

        if (stage.boss || stage.restReview) {
            questions = this.getRandomQuestions(stage.randomCount);
        }

        this.dungeon = {
            stageId: stage.id,
            questions,
            currentIndex: 0,
            correctInDungeon: 0,
            bossHp: questions.length,
            bossMaxHp: questions.length,
            timeLeft: stage.timeLimit || 0
        };

        this.selectedAnswer = null;
        this.answerLocked = false;
        this.showScreen("dungeonScreen");
        this.elements.dungeonTitle.textContent = `스테이지 ${stage.id}: ${stage.name}`;
        this.elements.bossPanel.hidden = !stage.boss;
        this.elements.timerPanel.hidden = !stage.timeLimit;

        if (stage.boss) {
            this.elements.bossName.textContent = "오염 골렘";
            this.updateBossHp();
        }

        if (stage.timeLimit) {
            this.startTimer(stage.timeLimit);
        } else {
            this.clearTimer();
        }

        this.renderQuestion();
        this.saveProgress();
    },

    getRandomQuestions(count) {
        return [...allQuestions]
            .sort(() => Math.random() - 0.5)
            .slice(0, count);
    },

    renderQuestion() {
        if (!this.dungeon) return;

        const stage = this.getCurrentStage();
        const question = this.dungeon.questions[this.dungeon.currentIndex];
        const currentNumber = this.dungeon.currentIndex + 1;
        const total = this.dungeon.questions.length;
        const progress = ((currentNumber - 1) / total) * 100;

        this.selectedAnswer = null;
        this.answerLocked = false;
        this.elements.quizCard.classList.remove("answer-correct", "answer-wrong");
        this.elements.questionCounter.textContent = `문제 ${currentNumber}/${total}`;
        this.elements.quizRewardHint.textContent = stage.boss ? "보스 체력 -1" : "에메랄드 +1";
        this.elements.quizProgress.style.width = `${progress}%`;
        this.elements.questionText.textContent = question.question;
        this.elements.answerOptions.innerHTML = "";
        this.elements.quizFeedback.className = "feedback";
        this.elements.quizFeedback.textContent = "";
        this.elements.submitAnswerButton.disabled = false;
        this.elements.submitAnswerButton.textContent = "정답 확인";

        question.options.forEach((optionText, index) => {
            const option = document.createElement("button");
            option.type = "button";
            option.className = "answer-option";
            option.textContent = optionText;
            option.addEventListener("click", () => this.selectAnswer(index));
            this.elements.answerOptions.appendChild(option);
        });

        this.updateQuest();
        this.updateHud();
    },

    selectAnswer(index) {
        if (this.answerLocked) return;
        this.selectedAnswer = index;

        Array.from(this.elements.answerOptions.children).forEach((option, optionIndex) => {
            option.classList.toggle("selected", optionIndex === index);
        });
    },

    submitAnswer() {
        if (!this.dungeon || this.answerLocked) return;

        if (this.selectedAnswer === null) {
            this.showQuizFeedback(false, "답을 하나 골라 주세요.");
            return;
        }

        const question = this.dungeon.questions[this.dungeon.currentIndex];
        const isCorrect = this.selectedAnswer === question.correct;
        const optionButtons = Array.from(this.elements.answerOptions.children);

        this.answerLocked = true;
        this.totalAnswers += 1;

        if (isCorrect) {
            this.correctAnswers += 1;
            this.dungeon.correctInDungeon += 1;
            this.score += this.getCurrentStage().boss ? 30 : 20;
            this.emeralds += this.getCurrentStage().boss ? 2 : 1;
            optionButtons[this.selectedAnswer].classList.add("correct");
            this.elements.quizCard.classList.add("answer-correct");
            this.showQuizFeedback(true, "정답이에요! 초능력 조각이 빛났어요.");

            if (this.getCurrentStage().boss) {
                this.dungeon.bossHp = Math.max(0, this.dungeon.bossHp - 1);
                this.updateBossHp();
            }

            this.updateHud();
            this.saveProgress();
            setTimeout(() => this.advanceQuestion(), 720);
            return;
        }

        optionButtons[this.selectedAnswer].classList.add("wrong");
        this.elements.quizCard.classList.add("answer-wrong");
        this.showQuizFeedback(false, "오답이에요. 다시 생각해 보고 골라 보세요.");
        this.updateHud();
        this.saveProgress();

        setTimeout(() => {
            this.elements.quizCard.classList.remove("answer-wrong");
            optionButtons[this.selectedAnswer]?.classList.remove("wrong", "selected");
            this.selectedAnswer = null;
            this.answerLocked = false;
        }, 520);
    },

    advanceQuestion() {
        if (!this.dungeon) return;

        if (this.dungeon.currentIndex >= this.dungeon.questions.length - 1) {
            this.elements.quizProgress.style.width = "100%";
            this.completeDungeon();
            return;
        }

        this.dungeon.currentIndex += 1;
        this.renderQuestion();
    },

    completeDungeon() {
        const stage = this.getCurrentStage();
        this.clearTimer();

        if (stage.boss) {
            this.showQuizFeedback(true, "복구 완료!");
        }

        setTimeout(() => this.finishStage(), stage.boss ? 800 : 450);
    },

    finishStage() {
        const stage = this.getCurrentStage();

        if (!this.completedStages.includes(stage.id)) {
            this.completedStages.push(stage.id);
        }

        if (!this.earnedPowers.includes(stage.power)) {
            this.earnedPowers.push(stage.power);
        }

        this.score += stage.clearBonus;
        this.emeralds += stage.rewardEmeralds;
        this.updateHud();
        this.saveProgress();

        this.showReward(stage.rewardTitle, stage.rewardText, () => {
            if (stage.id === this.stages.length) {
                this.completeGame();
            } else {
                this.enterStage(this.stageIndex + 1);
            }
        });
    },

    showQuizFeedback(isCorrect, message) {
        this.elements.quizFeedback.textContent = message;
        this.elements.quizFeedback.className = `feedback show ${isCorrect ? "correct" : "incorrect"}`;
    },

    startTimer(seconds) {
        this.clearTimer();
        this.dungeon.timeLeft = seconds;
        this.updateTimer();
        this.timerId = setInterval(() => {
            if (!this.dungeon) return;
            this.dungeon.timeLeft -= 1;
            this.updateTimer();

            if (this.dungeon.timeLeft <= 0) {
                this.failDungeon();
            }
        }, 1000);
    },

    updateTimer() {
        if (!this.dungeon) return;
        this.elements.timerText.textContent = `${Math.max(0, this.dungeon.timeLeft)}초`;
    },

    clearTimer() {
        if (this.timerId) {
            clearInterval(this.timerId);
            this.timerId = null;
        }
    },

    failDungeon() {
        this.clearTimer();
        this.showQuizFeedback(false, "시간이 끝났어요. 마을로 돌아가 다시 도전해요.");
        this.answerLocked = true;

        setTimeout(() => {
            const stage = this.getCurrentStage();
            this.player = { x: Math.max(1, stage.portal.x - 1), y: stage.portal.y };
            this.showScreen("worldScreen");
            this.renderWorld();
            this.updateQuest();
            this.saveProgress();
        }, 1700);
    },

    updateBossHp() {
        if (!this.dungeon) return;
        const hp = this.dungeon.bossHp;
        const maxHp = this.dungeon.bossMaxHp;
        const percent = maxHp === 0 ? 0 : (hp / maxHp) * 100;
        this.elements.bossHpText.textContent = `체력 ${hp}/${maxHp}`;
        this.elements.bossHpFill.style.width = `${percent}%`;
    },

    showDialogue(speaker, text, portrait = "villager", callback = null) {
        this.pendingDialogueCallback = callback;
        this.elements.dialogueSpeaker.textContent = speaker;
        this.elements.dialogueText.textContent = text;
        this.elements.dialoguePortrait.className = `dialogue-portrait ${portrait}`;
        this.elements.dialogueOverlay.hidden = false;
    },

    closeDialogue() {
        this.elements.dialogueOverlay.hidden = true;
        const callback = this.pendingDialogueCallback;
        this.pendingDialogueCallback = null;
        this.updateQuest();
        this.updateInteractPrompt();

        if (callback) callback();
    },

    showReward(title, text, callback) {
        this.pendingRewardCallback = callback;
        this.elements.rewardTitle.textContent = title;
        this.elements.rewardText.textContent = text;
        this.elements.rewardModal.hidden = false;
    },

    closeReward() {
        this.elements.rewardModal.hidden = true;
        const callback = this.pendingRewardCallback;
        this.pendingRewardCallback = null;
        if (callback) callback();
    },

    showStageTitle(stage) {
        clearTimeout(this.stageTitleTimer);
        this.elements.stageTitleNumber.textContent = `스테이지 ${stage.id}`;
        this.elements.stageTitleName.textContent = stage.name;
        this.elements.stageTitleOverlay.hidden = false;

        this.stageTitleTimer = setTimeout(() => {
            this.elements.stageTitleOverlay.hidden = true;
        }, 1450);
    },

    showTemporaryPrompt(message) {
        clearTimeout(this.promptTimer);
        this.elements.interactPrompt.textContent = message;
        this.elements.interactPrompt.hidden = false;

        this.promptTimer = setTimeout(() => {
            this.updateInteractPrompt();
        }, 1200);
    },

    updateQuest() {
        if (this.currentScreen === "dungeonScreen" && this.dungeon) {
            const stage = this.getCurrentStage();
            if (stage.boss) {
                this.elements.questText.textContent = "제한시간 안에 랜덤 문제 5개를 해결하기";
            } else {
                this.elements.questText.textContent = `퀴즈 ${this.dungeon.currentIndex + 1}/${this.dungeon.questions.length} 해결하기`;
            }
            return;
        }

        const stage = this.getCurrentStage();
        const stageState = this.getStageState();
        this.elements.worldStageTitle.textContent = `과학 탈출맵: 스테이지 ${stage.id} · ${stage.name}`;

        if (!stageState.npcTalked) {
            this.elements.questText.textContent = `${stage.npc.name} 만나기`;
        } else if (!stageState.monsterMet) {
            this.elements.questText.textContent = `${stage.monster.name} 근처에서 Q 키로 길 열기`;
        } else {
            this.elements.questText.textContent = "포탈 위로 이동해 퀴즈 던전 입장하기";
        }
    },

    updateHud() {
        const stage = this.getCurrentStage();
        const cleanPercent = Math.round((this.completedStages.length / this.stages.length) * 100);
        this.elements.stageInfo.textContent = `스테이지 ${stage.id}/6`;
        this.elements.score.textContent = `점수: ${this.score}`;
        this.elements.villageStatus.textContent = `마을 정화도: ${cleanPercent}%`;
        this.elements.emeraldCount.textContent = this.emeralds;
        this.elements.accuracyText.textContent = `${this.calculateAccuracy()}%`;

        if (this.elements.cleanMeterFill) {
            this.elements.cleanMeterFill.style.width = `${cleanPercent}%`;
        }

        this.stages.forEach((stageData, index) => {
            const slot = document.getElementById(`power${index + 1}`);
            if (slot) {
                slot.classList.toggle("earned", this.earnedPowers.includes(stageData.power));
            }
        });

        document.body.classList.remove("clean-0", "clean-1", "clean-2", "clean-3", "clean-4", "clean-5", "clean-6");
        document.body.classList.add(`clean-${this.completedStages.length}`);
        this.setupBackgrounds();
    },

    calculateAccuracy() {
        if (this.totalAnswers === 0) return 0;
        return Math.round((this.correctAnswers / this.totalAnswers) * 100);
    },

    setupBackgrounds() {
        const clean = this.completedStages.length;
        const backgrounds = [
            "linear-gradient(180deg, rgba(88, 50, 105, 0.48), rgba(79, 91, 50, 0.55)), linear-gradient(135deg, #6a8d5b, #5789a3)",
            "linear-gradient(180deg, rgba(90, 65, 92, 0.34), rgba(94, 115, 68, 0.42)), linear-gradient(135deg, #70a864, #66a7bb)",
            "linear-gradient(180deg, rgba(94, 78, 72, 0.22), rgba(96, 140, 82, 0.33)), linear-gradient(135deg, #78b86b, #6bb5c8)",
            "linear-gradient(180deg, rgba(90, 86, 64, 0.16), rgba(111, 157, 86, 0.22)), linear-gradient(135deg, #82c778, #72c4d5)",
            "linear-gradient(180deg, rgba(92, 99, 70, 0.12), rgba(123, 171, 90, 0.16)), linear-gradient(135deg, #8ad180, #86cfdf)",
            "linear-gradient(180deg, rgba(255, 243, 175, 0.16), rgba(143, 194, 98, 0.12)), linear-gradient(135deg, #8bd77f, #91d9e6)",
            "linear-gradient(180deg, rgba(255, 240, 166, 0.26), rgba(149, 214, 107, 0.18)), linear-gradient(135deg, #91db82, #9fe2ee)"
        ];
        this.elements.gameBackground.style.background = backgrounds[Math.min(clean, backgrounds.length - 1)];
    },

    showScreen(screenId) {
        this.currentScreen = screenId;
        document.body.classList.toggle("start-mode", screenId === "startScreen");
        document.body.classList.toggle("world-mode", screenId === "worldScreen");
        document.body.classList.toggle("dungeon-mode", screenId === "dungeonScreen");
        document.body.classList.toggle("ending-mode", screenId === "completeScreen");
        this.elements.screens.forEach((screen) => {
            screen.classList.toggle("active", screen.id === screenId);
        });
    },

    isOverlayOpen() {
        return !this.elements.dialogueOverlay.hidden ||
            !this.elements.rewardModal.hidden ||
            !this.elements.stageTitleOverlay.hidden ||
            !this.elements.portalTransition.hidden;
    },

    completeGame() {
        this.clearTimer();
        this.completedStages = [...new Set([...this.completedStages, ...this.stages.map((stage) => stage.id)])];
        this.updateHud();
        this.showScreen("completeScreen");
        this.elements.finalScore.textContent = `최종 점수: ${this.score}`;
        this.elements.finalAccuracy.textContent = `정확도: ${this.calculateAccuracy()}%`;
        this.elements.finalEmeralds.textContent = `에메랄드: ${this.emeralds}개`;
        this.elements.finalPowers.innerHTML = "";

        this.earnedPowers.forEach((power) => {
            const powerElement = document.createElement("div");
            powerElement.className = "final-power";
            powerElement.textContent = power;
            this.elements.finalPowers.appendChild(powerElement);
        });

        this.saveProgress(true);
    },

    hasSavedGame() {
        return Boolean(this.loadSavedGame());
    },

    loadSavedGame() {
        try {
            const rawData = localStorage.getItem(this.storageKey);
            return rawData ? JSON.parse(rawData) : null;
        } catch (error) {
            return null;
        }
    },

    applySavedGame(savedGame) {
        this.stageIndex = Math.min(Number(savedGame.stageIndex) || 0, this.stages.length - 1);
        this.score = Number(savedGame.score) || 0;
        this.emeralds = Number(savedGame.emeralds) || 0;
        this.totalAnswers = Number(savedGame.totalAnswers) || 0;
        this.correctAnswers = Number(savedGame.correctAnswers) || 0;
        this.completedStages = Array.isArray(savedGame.completedStages) ? savedGame.completedStages : [];
        this.earnedPowers = Array.isArray(savedGame.earnedPowers) ? savedGame.earnedPowers : [];
        this.stageWorldState = savedGame.stageWorldState && typeof savedGame.stageWorldState === "object"
            ? savedGame.stageWorldState
            : {};
    },

    saveProgress(finished = false) {
        try {
            localStorage.setItem(this.storageKey, JSON.stringify({
                stageIndex: this.stageIndex,
                score: this.score,
                emeralds: this.emeralds,
                totalAnswers: this.totalAnswers,
                correctAnswers: this.correctAnswers,
                completedStages: this.completedStages,
                earnedPowers: this.earnedPowers,
                stageWorldState: this.stageWorldState,
                finished
            }));
            this.elements.continueButton.hidden = false;
        } catch (error) {
            // 저장을 사용할 수 없는 브라우저에서도 게임은 계속 진행됩니다.
        }
    }
};

const explorationScenarios = {
    1: {
        requirements: { npcs: 2, fragments: 2, monsters: 1, pollution: 1, bridge: true, signs: 1 },
        npcs: [
            {
                id: "guide",
                name: "슬라임",
                label: "슬라임",
                portrait: "villager",
                className: "entity-npc",
                x: 3,
                y: 11,
                dialogue: "과학 초능력이 흩어졌어!\n산성 몬스터와 염기성 몬스터가\n초능력을 가져가기 전에 우리가 먼저 찾아야 해!!\n같이 찾아보자!\n힌트: 산성은 붉은 양배추 지시약을 붉은색 계열로 바꿔."
            },
            {
                id: "chemist",
                name: "실험 주민",
                label: "주민",
                portrait: "villager",
                className: "entity-npc",
                x: 7,
                y: 10,
                dialogue: "리트머스 종이처럼 색이 바뀌는 물질은 지시약이야.\n오염 블록을 정화할 때 이 단서가 필요할 거야."
            },
            {
                id: "guard",
                name: "동굴 주민",
                label: "주민",
                portrait: "villager",
                className: "entity-npc",
                x: 15,
                y: 9,
                dialogue: "포탈은 닫혀 있어.\n주민/친구 힌트 2개, 과학 조각 2개, 몬스터 해결, 오염 정화, 다리 복구가 필요해."
            }
        ],
        signs: [
            {
                id: "sign-bridge",
                name: "힌트 표지판",
                label: "표지",
                className: "entity-sign",
                x: 4,
                y: 8,
                dialogue: "강 건너편 비밀 공간에 파란 과학 조각이 있어.\n다리 버튼을 누르면 짧은 문제를 풀고 다리를 복구할 수 있어."
            }
        ],
        collectibles: [
            { id: "river-fragment", type: "fragment", name: "과학 조각", label: "조각", className: "entity-fragment", x: 6, y: 7, hidden: true, revealBy: "sign-bridge", amount: 1 },
            { id: "cave-fragment", type: "fragment", name: "과학 조각", label: "조각", className: "entity-fragment", x: 16, y: 4, hidden: true, revealBy: "acid-slime", amount: 1 },
            { id: "village-emerald", type: "emerald", name: "에메랄드", label: "보석", className: "entity-emerald", x: 5, y: 11, amount: 3 },
            { id: "forest-emerald", type: "emerald", name: "숨은 에메랄드", label: "보석", className: "entity-emerald", x: 12, y: 11, amount: 2 }
        ],
        bridge: {
            id: "river-bridge",
            label: "다리",
            className: "entity-bridge",
            x: 5,
            y: 8,
            button: { id: "bridge-button", name: "다리 버튼", label: "버튼", className: "entity-button", x: 3, y: 8 },
            quiz: {
                title: "다리 복구 퀴즈",
                question: {
                    question: "붉은 양배추 지시약은 산성 용액에서 붉은색 계열로 변하나요?",
                    options: ["맞다", "아니다"],
                    correct: 0
                }
            }
        },
        pollution: [
            {
                id: "acid-pollution",
                name: "오염 블록",
                label: "오염",
                className: "entity-pollution",
                x: 11,
                y: 5,
                requiredFragments: 1,
                quiz: {
                    title: "오염 정화 퀴즈",
                    question: quizBank.acidBase[4]
                }
            }
        ],
        monsters: [
            {
                id: "acid-slime",
                name: "산성 몬스터",
                label: "산성",
                portrait: "monster",
                className: "entity-monster entity-acid",
                x: 13,
                y: 5,
                rewardKey: true,
                quiz: {
                    title: "슬라임 퀴즈",
                    question: {
                        question: "염기성 용액에 붉은 양배추 지시약을 넣으면 푸른색이나 초록색 계열로 변하나요?",
                        options: ["맞다", "아니다"],
                        correct: 0
                    }
                },
                dialogue: "힌트를 모아 왔구나. 문제를 맞히면 길을 열고 숨은 조각도 보여 줄게!"
            }
        ],
        dangerZones: [
            { id: "acid-fog", x: 12, y: 5, safeAfterMonster: "acid-slime", message: "산성 안개가 따가워요. 힌트를 모아 몬스터를 먼저 해결하세요." },
            { id: "acid-fog-2", x: 13, y: 6, safeAfterMonster: "acid-slime", message: "위험 지역이에요. 다른 길로 돌아가거나 몬스터를 해결하세요." }
        ]
    },
    2: {
        requirements: { npcs: 2, fragments: 2, monsters: 1, pollution: 1, buttonPuzzle: true, signs: 1 },
        npcs: [
            { id: "driver", name: "운전 주민", label: "주민", portrait: "villager", className: "entity-npc", x: 3, y: 7, dialogue: "스테이지 2를 클리어 하러 가자!\n속력은 이동 거리 나누기 걸린 시간이야." },
            { id: "safety", name: "안전 주민", label: "주민", portrait: "villager", className: "entity-npc", x: 11, y: 7, dialogue: "안전벨트는 몸이 앞으로 튀어나가는 것을 줄여 줘.\n빠른 길보다 안전한 길이 먼저야." },
            { id: "runner", name: "빠른 주민", label: "주민", portrait: "villager", className: "entity-npc", x: 4, y: 2, dialogue: "버스가 2시간에 140km를 가면 시속 70km야.\n동굴 버튼은 거리, 시간, 속력 순서로 눌러!" }
        ],
        signs: [
            { id: "sign-motion", name: "속력 표지판", label: "표지", className: "entity-sign", x: 2, y: 6, dialogue: "갈림길 순서: 거리 → 시간 → 속력.\n순서가 틀리면 버튼 퍼즐이 다시 잠겨." }
        ],
        collectibles: [
            { id: "motion-fragment-1", type: "fragment", name: "과학 조각", label: "조각", className: "entity-fragment", x: 5, y: 2, hidden: true, revealBy: "sign-motion", amount: 1 },
            { id: "motion-fragment-2", type: "fragment", name: "과학 조각", label: "조각", className: "entity-fragment", x: 12, y: 4, hidden: true, revealBy: "fast-slime", amount: 1 },
            { id: "motion-emerald", type: "emerald", name: "에메랄드", label: "보석", className: "entity-emerald", x: 6, y: 7, amount: 4 }
        ],
        buttonPuzzle: {
            sequence: ["distance", "time", "speed"],
            buttons: [
                { id: "distance", name: "거리 버튼", label: "거리", className: "entity-button", x: 5, y: 7 },
                { id: "time", name: "시간 버튼", label: "시간", className: "entity-button", x: 7, y: 7 },
                { id: "speed", name: "속력 버튼", label: "속력", className: "entity-button", x: 9, y: 7 }
            ]
        },
        pollution: [
            { id: "speed-pollution", name: "막힌 길", label: "오염", className: "entity-pollution", x: 10, y: 4, requiredFragments: 1, quiz: { title: "과속 방지 퀴즈", question: quizBank.motion[4] } }
        ],
        monsters: [
            { id: "fast-slime", name: "빠른 슬라임", label: "슬라임", portrait: "monster", className: "entity-monster", x: 8, y: 3, rewardKey: true, dialogue: "속력을 알면 이 길을 통과시켜 줄게!", quiz: { title: "빠른 슬라임 퀴즈", question: quizBank.motion[2] } }
        ],
        dangerZones: [
            { id: "fast-road", x: 8, y: 4, safeAfterMonster: "fast-slime", message: "빠른 슬라임의 길이에요. 안전 힌트를 먼저 모으세요." }
        ]
    },
    3: {
        requirements: { npcs: 2, fragments: 2, monsters: 1, pollution: 1, signs: 1 },
        npcs: [
            { id: "forest-guide", name: "숲 주민", label: "주민", portrait: "villager", className: "entity-npc", x: 3, y: 7, dialogue: "이번에도 스테이지 3을 클리어 하자!!\n잎의 기공으로 물이 수증기가 되어 나가는 것을 증산작용이라고 해." },
            { id: "root-friend", name: "뿌리 주민", label: "주민", portrait: "villager", className: "entity-npc", x: 11, y: 7, dialogue: "뿌리는 식물을 지탱하고 물과 무기 양분을 흡수해.\n줄기는 물과 양분이 이동하는 통로야." }
        ],
        signs: [
            { id: "sign-plant", name: "숲 표지판", label: "표지", className: "entity-sign", x: 4, y: 7, dialogue: "비밀 꽃밭 근처에 과학 조각이 숨어 있어.\n꽃가루가 암술로 옮겨지는 과정은 수분이야." }
        ],
        collectibles: [
            { id: "plant-fragment-1", type: "fragment", name: "과학 조각", label: "조각", className: "entity-fragment", x: 5, y: 2, hidden: true, revealBy: "sign-plant", amount: 1 },
            { id: "plant-fragment-2", type: "fragment", name: "과학 조각", label: "조각", className: "entity-fragment", x: 12, y: 4, hidden: true, revealBy: "vine-monster", amount: 1 },
            { id: "plant-emerald", type: "emerald", name: "에메랄드", label: "보석", className: "entity-emerald", x: 8, y: 7, amount: 4 }
        ],
        pollution: [
            { id: "vine-pollution", name: "시든 흙", label: "오염", className: "entity-pollution", x: 10, y: 4, requiredFragments: 1, quiz: { title: "시든 흙 정화", question: quizBank.plant[3] } }
        ],
        monsters: [
            { id: "vine-monster", name: "덩굴 몬스터", label: "덩굴", portrait: "monster", className: "entity-monster", x: 8, y: 3, rewardKey: true, dialogue: "식물의 기능을 알면 덩굴 길을 열어 줄게.", quiz: { title: "덩굴 퀴즈", question: quizBank.plant[7] } }
        ],
        dangerZones: [
            { id: "thorn", x: 8, y: 4, safeAfterMonster: "vine-monster", message: "가시 덩굴 지역이에요. 덩굴 몬스터를 먼저 해결하세요." }
        ]
    },
    4: {
        requirements: { npcs: 2, fragments: 2, monsters: 1, pollution: 1, signs: 1 },
        npcs: [
            { id: "star-guide", name: "별빛 주민", label: "주민", portrait: "villager", className: "entity-npc", x: 3, y: 7, dialogue: "과학 초능력을 얻으러 가자!\n지구는 지축을 기준으로 서쪽에서 동쪽으로 자전해." },
            { id: "sun-watcher", name: "태양 주민", label: "주민", portrait: "villager", className: "entity-npc", x: 11, y: 7, dialogue: "태양은 동쪽에서 서쪽으로 이동하는 것처럼 보여.\n그 까닭은 지구가 자전하기 때문이야." }
        ],
        signs: [
            { id: "sign-earth", name: "천문 표지판", label: "표지", className: "entity-sign", x: 4, y: 7, dialogue: "그림자 길의 비밀: 태양 빛을 받지 못하는 쪽은 밤이야.\n숨은 조각은 별빛 길 근처에 있어." }
        ],
        collectibles: [
            { id: "earth-fragment-1", type: "fragment", name: "과학 조각", label: "조각", className: "entity-fragment", x: 5, y: 2, hidden: true, revealBy: "sign-earth", amount: 1 },
            { id: "earth-fragment-2", type: "fragment", name: "과학 조각", label: "조각", className: "entity-fragment", x: 12, y: 4, hidden: true, revealBy: "shadow-monster", amount: 1 },
            { id: "earth-emerald", type: "emerald", name: "에메랄드", label: "보석", className: "entity-emerald", x: 9, y: 7, amount: 5 }
        ],
        pollution: [
            { id: "shadow-pollution", name: "그림자 오염", label: "오염", className: "entity-pollution", x: 10, y: 4, requiredFragments: 1, quiz: { title: "그림자 정화", question: quizBank.earth[6] } }
        ],
        monsters: [
            { id: "shadow-monster", name: "그림자 몬스터", label: "그림자", portrait: "monster", className: "entity-monster entity-base", x: 8, y: 3, rewardKey: true, dialogue: "낮과 밤의 비밀을 말해 봐!", quiz: { title: "그림자 퀴즈", question: quizBank.earth[8] } }
        ],
        dangerZones: [
            { id: "night-zone", x: 8, y: 4, safeAfterMonster: "shadow-monster", message: "그림자가 짙어요. 태양 주민의 힌트를 먼저 들어 보세요." }
        ]
    },
    5: {
        requirements: { npcs: 1, fragments: 2, monsters: 1, pollution: 1, signs: 1 },
        npcs: [
            { id: "iron-golem", name: "철골렘", label: "철골렘", portrait: "golem", className: "entity-golem", x: 3, y: 7, dialogue: "스테이지 5를 깨자!\n지금까지 모은 힌트를 모두 사용해야 보스 포탈이 열린다." }
        ],
        signs: [
            { id: "sign-boss", name: "보스 표지판", label: "표지", className: "entity-sign", x: 4, y: 7, dialogue: "중간보스는 랜덤 문제를 낸다.\n산과 염기, 운동, 식물, 지구의 힌트를 모두 떠올려!" }
        ],
        collectibles: [
            { id: "boss-fragment-1", type: "fragment", name: "과학 조각", label: "조각", className: "entity-fragment", x: 5, y: 2, hidden: true, revealBy: "sign-boss", amount: 1 },
            { id: "boss-fragment-2", type: "fragment", name: "과학 조각", label: "조각", className: "entity-fragment", x: 12, y: 4, hidden: true, revealBy: "polluted-golem", amount: 1 },
            { id: "boss-emerald", type: "emerald", name: "에메랄드", label: "보석", className: "entity-emerald", x: 9, y: 7, amount: 6 }
        ],
        pollution: [
            { id: "boss-pollution", name: "보스 오염핵", label: "오염", className: "entity-pollution", x: 10, y: 4, requiredFragments: 1, quiz: { title: "오염핵 정화", question: quizBank.motion[5] } }
        ],
        monsters: [
            { id: "polluted-golem", name: "오염 골렘", label: "보스", portrait: "monster", className: "entity-monster entity-boss", x: 8, y: 3, rewardKey: true, dialogue: "12초 안에 맞히면 보스 포탈의 봉인을 풀어 주지!", quiz: { title: "제한시간 미니 보스", timeLimit: 12, question: quizBank.earth[7] } }
        ],
        dangerZones: [
            { id: "boss-warning", x: 8, y: 4, safeAfterMonster: "polluted-golem", message: "보스의 압력이 강해요. 철골렘과 표지판 힌트를 먼저 확인하세요." }
        ]
    },
    6: {
        requirements: { npcs: 2, fragments: 1, monsters: 1, signs: 1 },
        npcs: [
            { id: "ending-villager", name: "주민", label: "주민", portrait: "villager", className: "entity-npc", x: 3, y: 7, dialogue: "와 축하해!\n마지막 휴식 포탈은 배운 내용을 가볍게 확인하면 열려." },
            { id: "feast-friend", name: "잔치 주민", label: "주민", portrait: "villager", className: "entity-resident", x: 10, y: 7, dialogue: "고기를 나눠 먹으며 쉬자!\n그래도 과학 초능력 조각 하나는 찾아야 해." }
        ],
        signs: [
            { id: "sign-rest", name: "휴식 표지판", label: "표지", className: "entity-sign", x: 4, y: 7, dialogue: "마지막 조각은 잔치 자리 근처에 숨어 있어.\n정화된 슬라임은 이제 친구야." }
        ],
        collectibles: [
            { id: "rest-fragment", type: "fragment", name: "과학 조각", label: "조각", className: "entity-fragment", x: 7, y: 7, hidden: true, revealBy: "sign-rest", amount: 1 },
            { id: "rest-emerald", type: "emerald", name: "에메랄드", label: "보석", className: "entity-emerald", x: 5, y: 7, amount: 6 }
        ],
        monsters: [
            { id: "friendly-slime", name: "정화된 슬라임", label: "친구", portrait: "monster", className: "entity-monster", x: 8, y: 7, dialogue: "마지막 확인 문제야. 맞히면 휴식 포탈을 열어 줄게!", quiz: { title: "휴식 확인 퀴즈", question: quizBank.plant[5] } }
        ]
    }
};

const baseCacheElements = game.cacheElements.bind(game);

Object.assign(game, {
    lastCorrectSlot: null,
    activeFieldChallenge: null,

    cacheElements() {
        baseCacheElements();
        this.elements.worldInventory = document.getElementById("worldInventory");
    },

    resetRuntimeState() {
        this.stageIndex = 0;
        this.score = 0;
        this.emeralds = 0;
        this.totalAnswers = 0;
        this.correctAnswers = 0;
        this.completedStages = [];
        this.earnedPowers = [];
        this.stageWorldState = {};
        this.player = { x: 2, y: 7 };
        this.dungeon = null;
        this.activeFieldChallenge = null;
        this.selectedAnswer = null;
        this.answerLocked = false;
        this.nearbyEntity = null;
        this.isTransitioning = false;
        this.lastCorrectSlot = null;
        this.clearTimer();
    },

    getScenario(stage = this.getCurrentStage()) {
        return explorationScenarios[stage.id];
    },

    ensureStageState(stageId) {
        const state = this.stageWorldState[stageId] || {};
        this.stageWorldState[stageId] = {
            talkedNpcIds: Array.isArray(state.talkedNpcIds) ? state.talkedNpcIds : [],
            readSignIds: Array.isArray(state.readSignIds) ? state.readSignIds : [],
            collectedIds: Array.isArray(state.collectedIds) ? state.collectedIds : [],
            minedIds: Array.isArray(state.minedIds) ? state.minedIds : [],
            solvedMonsterIds: Array.isArray(state.solvedMonsterIds) ? state.solvedMonsterIds : [],
            clearedPollutionIds: Array.isArray(state.clearedPollutionIds) ? state.clearedPollutionIds : [],
            dangerHits: Array.isArray(state.dangerHits) ? state.dangerHits : [],
            buttonSequence: Array.isArray(state.buttonSequence) ? state.buttonSequence : [],
            fragments: Number(state.fragments) || 0,
            keys: Number(state.keys) || 0,
            bridgeFixed: Boolean(state.bridgeFixed),
            buttonPuzzleSolved: Boolean(state.buttonPuzzleSolved),
            portalOpen: Boolean(state.portalOpen),
            portalAnnounced: Boolean(state.portalAnnounced),
            needsHint: Boolean(state.needsHint),
            introShown: Boolean(state.introShown)
        };
    },

    enterStage(index, resumed = false) {
        this.clearTimer();
        this.stageIndex = Math.min(index, this.stages.length - 1);
        const stage = this.getCurrentStage();
        const scenario = this.getScenario(stage);
        this.ensureStageState(stage.id);
        this.player = { ...stage.start };
        this.dungeon = null;
        this.activeFieldChallenge = null;
        this.selectedAnswer = null;
        this.answerLocked = false;

        this.showScreen("worldScreen");
        this.renderWorld();
        this.updateHud();
        this.updateQuest();
        this.showStageTitle(stage);
        this.saveProgress();

        const state = this.getStageState();
        const introNpc = scenario?.npcs?.[0] || stage.npc;
        if (introNpc && !state.introShown && !resumed) {
            state.introShown = true;
            setTimeout(() => {
                if (this.currentScreen === "worldScreen") {
                    this.showDialogue(introNpc.name, introNpc.dialogue, introNpc.portrait);
                }
            }, 1550);
        }
    },

    renderWorld() {
        const stage = this.getCurrentStage();
        const map = stage.map;
        const worldMap = this.elements.worldMap;
        worldMap.innerHTML = "";
        worldMap.style.setProperty("--cols", this.cols);
        worldMap.style.setProperty("--rows", this.rows);

        for (let y = 0; y < this.rows; y += 1) {
            for (let x = 0; x < this.cols; x += 1) {
                const tile = document.createElement("div");
                tile.className = `tile ${this.getTileClass(map[y]?.[x] || "G", x, y)}`;
                worldMap.appendChild(tile);
            }
        }

        this.getVisibleEntities().forEach((entity) => {
            const entityElement = document.createElement("div");
            entityElement.className = `map-sprite entity ${entity.className}`;
            entityElement.dataset.entityId = entity.id || entity.kind || entity.label;
            entityElement.style.setProperty("--x", entity.x);
            entityElement.style.setProperty("--y", entity.y);

            const label = document.createElement("span");
            label.className = "entity-label";
            label.textContent = entity.label;
            entityElement.appendChild(label);
            worldMap.appendChild(entityElement);
        });

        const playerElement = document.createElement("div");
        playerElement.className = "map-sprite player";
        worldMap.appendChild(playerElement);
        this.playerElement = playerElement;
        this.renderPlayerPosition();
        this.updateInteractPrompt();
    },

    getVisibleEntities() {
        const stage = this.getCurrentStage();
        const scenario = this.getScenario(stage);
        const state = this.getStageState();
        if (!scenario) {
            return [
                { ...stage.npc, id: "npc", kind: "npc" },
                { ...stage.monster, id: "monster", kind: "monster" },
                { ...stage.portal, id: "portal", kind: "portal", className: "entity-portal" }
            ];
        }

        const entities = [];
        scenario.npcs.forEach((npc) => entities.push({ ...npc, kind: "npc" }));
        scenario.signs?.forEach((sign) => entities.push({ ...sign, kind: "sign" }));

        scenario.collectibles?.forEach((item) => {
            if (!state.collectedIds.includes(item.id) && this.isCollectibleVisible(item, state)) {
                entities.push({ ...item, kind: "collectible" });
            }
        });

        scenario.pollution?.forEach((pollution) => {
            if (!state.clearedPollutionIds.includes(pollution.id)) {
                entities.push({ ...pollution, kind: "pollution" });
            }
        });

        scenario.monsters?.forEach((monster) => {
            if (!state.solvedMonsterIds.includes(monster.id)) {
                entities.push({ ...monster, kind: "monster" });
            }
        });

        if (scenario.bridge?.button && !state.bridgeFixed) {
            entities.push({ ...scenario.bridge.button, kind: "bridgeButton" });
        }
        if (scenario.bridge && state.bridgeFixed) {
            entities.push({ id: scenario.bridge.id, name: "복구된 다리", label: scenario.bridge.label, className: scenario.bridge.className, x: scenario.bridge.x, y: scenario.bridge.y, kind: "bridge" });
        }

        scenario.buttonPuzzle?.buttons?.forEach((button) => {
            entities.push({ ...button, kind: "sequenceButton" });
        });

        scenario.dangerZones?.forEach((zone) => {
            if (!state.solvedMonsterIds.includes(zone.safeAfterMonster)) {
                entities.push({ id: zone.id, name: "위험 경고", label: "위험", className: "entity-warning", x: zone.x, y: zone.y, kind: "warning" });
            }
        });

        (stage.decorations || []).forEach((decoration, index) => {
            entities.push({ ...decoration, id: `decoration-${index}`, kind: "decoration" });
        });

        const portalOpen = state.portalOpen || this.arePortalRequirementsMet();
        const portalClass = portalOpen ? "entity-portal" : "entity-portal locked";
        entities.push({ ...stage.portal, id: "portal", name: portalOpen ? "열린 포탈" : "잠긴 포탈", kind: "portal", className: portalClass });
        return entities;
    },

    isCollectibleVisible(item, state) {
        if (!item.hidden) return true;
        if (item.revealBy && (state.readSignIds.includes(item.revealBy) || state.solvedMonsterIds.includes(item.revealBy))) return true;
        return Math.abs(item.x - this.player.x) + Math.abs(item.y - this.player.y) <= 2;
    },

    movePlayer(direction) {
        if (this.currentScreen !== "worldScreen" || this.isOverlayOpen() || this.isTransitioning) return;

        const moves = {
            up: { x: 0, y: -1 },
            down: { x: 0, y: 1 },
            left: { x: -1, y: 0 },
            right: { x: 1, y: 0 }
        };
        const delta = moves[direction];
        if (!delta) return;

        const previousPosition = { ...this.player };
        const nextPosition = {
            x: this.player.x + delta.x,
            y: this.player.y + delta.y
        };

        if (this.isBlocked(nextPosition.x, nextPosition.y)) {
            this.showTemporaryPrompt("길이 막혀 있어요. 주민/장치는 Q, 나무와 돌은 E 키를 눌러 해결해 보세요.");
            return;
        }

        this.player = nextPosition;
        this.renderPlayerPosition();
        this.checkCollectibles();
        this.checkDangerZone(previousPosition);
        this.updateInteractPrompt();
        this.checkPortalEntry();
        this.saveProgress();
    },

    isBlocked(x, y) {
        if (x < 0 || y < 0 || x >= this.cols || y >= this.rows) return true;

        const stage = this.getCurrentStage();
        const scenario = this.getScenario(stage);
        const state = this.getStageState();
        const tileType = stage.map[y]?.[x] || "G";
        const bridgeIsHere = scenario?.bridge && state.bridgeFixed && scenario.bridge.x === x && scenario.bridge.y === y;
        if (this.isMinedTile(x, y)) return false;
        if (["T", "R", "W"].includes(tileType) && !bridgeIsHere) return true;

        if (scenario?.monsters?.some((monster) => !state.solvedMonsterIds.includes(monster.id) && monster.x === x && monster.y === y)) return true;
        if (scenario?.pollution?.some((pollution) => !state.clearedPollutionIds.includes(pollution.id) && pollution.x === x && pollution.y === y)) return true;

        return (stage.decorations || []).some((decoration) => decoration.x === x && decoration.y === y);
    },

    checkCollectibles() {
        const scenario = this.getScenario();
        if (!scenario) return;
        const state = this.getStageState();

        scenario.collectibles?.forEach((item) => {
            const isHere = item.x === this.player.x && item.y === this.player.y;
            if (!isHere || state.collectedIds.includes(item.id) || !this.isCollectibleVisible(item, state)) return;

            state.collectedIds.push(item.id);
            if (item.type === "fragment") {
                state.fragments += item.amount || 1;
                this.score += 15;
                this.showTemporaryPrompt("과학 조각을 획득했어요!");
            } else if (item.type === "key") {
                state.keys += item.amount || 1;
                this.showTemporaryPrompt("포탈 열쇠를 획득했어요!");
            } else {
                this.emeralds += item.amount || 1;
                this.score += 10;
                this.showTemporaryPrompt(`에메랄드 ${item.amount || 1}개 획득!`);
            }
            this.updateHud();
            this.updateQuest();
            this.checkPortalActivation();
            this.renderWorld();
        });
    },

    checkDangerZone(previousPosition) {
        const scenario = this.getScenario();
        if (!scenario?.dangerZones) return;
        const state = this.getStageState();
        const zone = scenario.dangerZones.find((danger) => danger.x === this.player.x && danger.y === this.player.y);
        if (!zone || state.solvedMonsterIds.includes(zone.safeAfterMonster)) return;

        if (!state.dangerHits.includes(zone.id)) {
            state.dangerHits.push(zone.id);
            this.applyFailurePenalty("위험 지역에 들어가 오염도가 올라갔어요.");
        }
        this.player = previousPosition;
        this.renderWorld();
        this.showTemporaryPrompt(zone.message);
    },

    updateInteractPrompt() {
        const entity = this.getNearbyInteractiveEntity();
        const mineTarget = this.getMineTarget();
        this.nearbyEntity = entity;

        if (!entity && !mineTarget) {
            this.elements.interactPrompt.hidden = true;
            return;
        }

        const actionNames = {
            npc: "대화",
            sign: "읽기",
            monster: "해결",
            pollution: "정화",
            bridgeButton: "누르기",
            sequenceButton: "누르기",
            portal: "확인"
        };
        const prompts = [];
        if (entity) {
            prompts.push(`Q 키: ${entity.name} ${actionNames[entity.kind] || "확인"}`);
        }
        if (mineTarget) {
            prompts.push(`E 키: ${mineTarget.tile === "T" ? "나무 캐기" : "돌 캐기"}`);
        }
        this.elements.interactPrompt.textContent = prompts.join(" · ");
        this.elements.interactPrompt.hidden = false;
    },

    getNearbyInteractiveEntity() {
        const candidates = this.getVisibleEntities().filter((entity) => (
            ["npc", "sign", "monster", "pollution", "bridgeButton", "sequenceButton", "portal"].includes(entity.kind)
        ));

        const priority = {
            portal: 1,
            pollution: 2,
            monster: 3,
            sign: 4,
            npc: 5,
            bridgeButton: 6,
            sequenceButton: 6
        };

        return candidates.map((entity) => ({
            ...entity,
            distance: Math.abs(entity.x - this.player.x) + Math.abs(entity.y - this.player.y)
        })).filter((entity) => (
            entity.distance <= 1
        )).sort((a, b) => (
            a.distance - b.distance || (priority[a.kind] || 9) - (priority[b.kind] || 9)
        ))[0] || null;
    },

    interact() {
        const entity = this.nearbyEntity || this.getNearbyInteractiveEntity();
        if (!entity) {
            this.useTool();
            return;
        }

        const handlers = {
            npc: () => this.handleNpc(entity),
            sign: () => this.handleSign(entity),
            monster: () => this.handleMonster(entity),
            pollution: () => this.handlePollution(entity),
            bridgeButton: () => this.handleBridgeButton(entity),
            sequenceButton: () => this.handleSequenceButton(entity),
            portal: () => this.handlePortal()
        };

        handlers[entity.kind]?.();
    },

    handleNpc(npc) {
        const state = this.getStageState();
        if (!state.talkedNpcIds.includes(npc.id)) {
            state.talkedNpcIds.push(npc.id);
            this.score += 5;
        }
        state.needsHint = false;
        this.showDialogue(npc.name, npc.dialogue, npc.portrait, () => {
            this.updateHud();
            this.updateQuest();
            this.checkPortalActivation();
            this.renderWorld();
            this.saveProgress();
        });
    },

    handleSign(sign) {
        const state = this.getStageState();
        if (!state.readSignIds.includes(sign.id)) {
            state.readSignIds.push(sign.id);
            this.score += 5;
        }
        state.needsHint = false;
        this.showDialogue(sign.name, sign.dialogue, "villager", () => {
            this.updateQuest();
            this.checkPortalActivation();
            this.renderWorld();
            this.saveProgress();
        });
    },

    handleMonster(monster) {
        const state = this.getStageState();
        if (state.needsHint) {
            this.showTemporaryPrompt("방금 실패했어요. 주민이나 표지판에서 힌트를 다시 찾아보세요.");
            return;
        }

        this.showDialogue(monster.name, monster.dialogue, monster.portrait, () => {
            this.startFieldChallenge(monster.quiz, {
                onCorrect: () => {
                    if (!state.solvedMonsterIds.includes(monster.id)) {
                        state.solvedMonsterIds.push(monster.id);
                        if (monster.rewardKey) state.keys += 1;
                        this.score += 35;
                        this.emeralds += 2;
                    }
                    state.needsHint = false;
                    this.showTemporaryPrompt("몬스터가 길을 열었어요!");
                },
                onWrong: () => {
                    state.needsHint = true;
                    this.applyFailurePenalty("몬스터가 다시 길을 막았어요.");
                }
            });
        });
    },

    handlePollution(pollution) {
        const state = this.getStageState();
        if (state.fragments < (pollution.requiredFragments || 0)) {
            this.showDialogue("오염 블록", `과학 조각 ${pollution.requiredFragments}개가 필요해요.\n맵을 더 탐험해 숨은 조각을 찾아보세요.`, "monster");
            return;
        }
        if (state.needsHint) {
            this.showTemporaryPrompt("정화에 실패했어요. 힌트를 다시 확인해야 해요.");
            return;
        }

        this.startFieldChallenge(pollution.quiz, {
            onCorrect: () => {
                if (!state.clearedPollutionIds.includes(pollution.id)) {
                    state.clearedPollutionIds.push(pollution.id);
                    this.score += 30;
                    this.emeralds += 2;
                }
                state.needsHint = false;
                this.showTemporaryPrompt("오염 블록이 사라졌어요!");
            },
            onWrong: () => {
                state.needsHint = true;
                this.applyFailurePenalty("오염 블록이 더 짙어졌어요.");
            }
        });
    },

    handleBridgeButton() {
        const scenario = this.getScenario();
        const state = this.getStageState();
        if (!scenario?.bridge || state.bridgeFixed) return;
        if (state.needsHint) {
            this.showTemporaryPrompt("버튼이 잠겼어요. 표지판이나 주민 힌트를 다시 확인하세요.");
            return;
        }

        this.startFieldChallenge(scenario.bridge.quiz, {
            onCorrect: () => {
                state.bridgeFixed = true;
                state.needsHint = false;
                this.score += 25;
                this.showTemporaryPrompt("강 위에 다리가 복구되었어요!");
            },
            onWrong: () => {
                state.needsHint = true;
                this.applyFailurePenalty("다리 버튼이 잠겼어요. 표지판 힌트를 다시 확인하세요.");
            }
        });
    },

    handleSequenceButton(button) {
        const scenario = this.getScenario();
        const state = this.getStageState();
        const sequence = scenario?.buttonPuzzle?.sequence || [];
        if (state.buttonPuzzleSolved) {
            this.showTemporaryPrompt("버튼 퍼즐은 이미 해결했어요.");
            return;
        }

        const expected = sequence[state.buttonSequence.length];
        if (button.id === expected) {
            state.buttonSequence.push(button.id);
            this.score += 5;
            if (state.buttonSequence.length === sequence.length) {
                state.buttonPuzzleSolved = true;
                state.needsHint = false;
                this.score += 25;
                this.showTemporaryPrompt("버튼 순서가 맞았어요. 막힌 길이 열렸어요!");
            } else {
                this.showTemporaryPrompt(`좋아요! ${state.buttonSequence.length}/${sequence.length} 다음 버튼을 찾아 누르세요.`);
            }
        } else {
            state.buttonSequence = [];
            this.showTemporaryPrompt("순서가 틀렸어요. 처음부터 다시 눌러 보세요! (힌트: 거리→시간→속력)");
        }

        this.updateHud();
        this.updateQuest();
        this.checkPortalActivation();
        this.renderWorld();
        this.saveProgress();
    },

    startFieldChallenge(challenge, callbacks) {
        this.clearTimer();
        const preparedQuestions = this.prepareQuestionSet([challenge.question]);
        this.activeFieldChallenge = callbacks;
        this.dungeon = {
            mode: "field",
            questions: preparedQuestions,
            currentIndex: 0,
            correctInDungeon: 0,
            bossHp: 0,
            bossMaxHp: 0,
            timeLeft: challenge.timeLimit || 0
        };

        this.selectedAnswer = null;
        this.answerLocked = false;
        this.showScreen("dungeonScreen");
        this.elements.dungeonTitle.textContent = challenge.title || "현장 퀴즈";
        this.elements.bossPanel.hidden = true;
        this.elements.timerPanel.hidden = !challenge.timeLimit;
        if (challenge.timeLimit) {
            this.startTimer(challenge.timeLimit);
        }
        this.renderQuestion();
    },

    startDungeon() {
        const stage = this.getCurrentStage();
        let pool = stage.questions || allQuestions;
        let questionCount = Math.min(stage.finalQuestionCount || 5, pool.length);

        if (stage.boss) {
            pool = allQuestions;
            questionCount = stage.randomCount || 5;
        }
        if (stage.restReview) {
            pool = allQuestions;
            questionCount = stage.randomCount || 3;
        }

        const questions = this.prepareQuestionSet(this.getRandomQuestions(questionCount, pool));
        this.dungeon = {
            mode: "dungeon",
            stageId: stage.id,
            questions,
            currentIndex: 0,
            correctInDungeon: 0,
            bossHp: questions.length,
            bossMaxHp: questions.length,
            timeLeft: stage.timeLimit || 0
        };

        this.selectedAnswer = null;
        this.answerLocked = false;
        this.showScreen("dungeonScreen");
        this.elements.dungeonTitle.textContent = stage.boss ? "중간보스 최종 시험" : `스테이지 ${stage.id}: 최종 시험`;
        this.elements.bossPanel.hidden = !stage.boss;
        this.elements.timerPanel.hidden = !stage.timeLimit;

        if (stage.boss) {
            this.elements.bossName.textContent = "오염 골렘";
            this.updateBossHp();
        }

        if (stage.timeLimit) {
            this.startTimer(stage.timeLimit);
        } else {
            this.clearTimer();
        }

        this.renderQuestion();
        this.saveProgress();
    },

    getRandomQuestions(count, pool = allQuestions) {
        return [...pool].sort(() => Math.random() - 0.5).slice(0, count);
    },

    prepareQuestionSet(questions) {
        let avoidSlot = this.lastCorrectSlot;
        const prepared = questions.map((question) => {
            const shuffled = this.shuffleQuestion(question, avoidSlot);
            avoidSlot = shuffled.correct;
            return shuffled;
        });
        this.lastCorrectSlot = avoidSlot;
        return prepared;
    },

    shuffleQuestion(question, avoidSlot = null) {
        const order = question.options.map((_, index) => index);
        for (let i = order.length - 1; i > 0; i -= 1) {
            const j = Math.floor(Math.random() * (i + 1));
            [order[i], order[j]] = [order[j], order[i]];
        }

        let correctSlot = order.indexOf(question.correct);
        if (avoidSlot !== null && correctSlot === avoidSlot && order.length > 1) {
            const swapSlot = order.findIndex((_, index) => index !== correctSlot && index !== avoidSlot);
            const fallbackSlot = swapSlot >= 0 ? swapSlot : (correctSlot + 1) % order.length;
            [order[correctSlot], order[fallbackSlot]] = [order[fallbackSlot], order[correctSlot]];
            correctSlot = order.indexOf(question.correct);
        }

        return {
            question: question.question,
            options: order.map((index) => question.options[index]),
            correct: correctSlot
        };
    },

    renderQuestion() {
        if (!this.dungeon) return;

        const stage = this.getCurrentStage();
        const question = this.dungeon.questions[this.dungeon.currentIndex];
        const currentNumber = this.dungeon.currentIndex + 1;
        const total = this.dungeon.questions.length;
        const progress = ((currentNumber - 1) / total) * 100;
        const isField = this.dungeon.mode === "field";

        this.selectedAnswer = null;
        this.answerLocked = false;
        this.elements.quizCard.classList.remove("answer-correct", "answer-wrong");
        this.elements.questionCounter.textContent = isField ? "현장 문제" : `문제 ${currentNumber}/${total}`;
        this.elements.quizRewardHint.textContent = isField ? "월드 보상" : (stage.boss ? "보스 체력 -1" : "에메랄드 +1");
        this.elements.quizProgress.style.width = `${progress}%`;
        this.elements.questionText.textContent = question.question;
        this.elements.answerOptions.innerHTML = "";
        this.elements.quizFeedback.className = "feedback";
        this.elements.quizFeedback.textContent = "";
        this.elements.submitAnswerButton.disabled = false;
        this.elements.submitAnswerButton.textContent = "정답 확인";

        question.options.forEach((optionText, index) => {
            const option = document.createElement("button");
            option.type = "button";
            option.className = "answer-option";
            option.textContent = optionText;
            option.addEventListener("click", () => this.selectAnswer(index));
            this.elements.answerOptions.appendChild(option);
        });

        this.updateQuest();
        this.updateHud();
    },

    submitAnswer() {
        if (!this.dungeon || this.answerLocked) return;

        if (this.selectedAnswer === null) {
            this.showQuizFeedback(false, "답을 하나 골라 주세요.");
            return;
        }

        const question = this.dungeon.questions[this.dungeon.currentIndex];
        const isCorrect = this.selectedAnswer === question.correct;
        const isField = this.dungeon.mode === "field";
        const optionButtons = Array.from(this.elements.answerOptions.children);

        this.answerLocked = true;
        this.totalAnswers += 1;

        if (isCorrect) {
            this.correctAnswers += 1;
            this.dungeon.correctInDungeon += 1;
            this.score += this.getCurrentStage().boss ? 30 : 20;
            this.emeralds += isField ? 1 : (this.getCurrentStage().boss ? 2 : 1);
            optionButtons[this.selectedAnswer].classList.add("correct");
            this.elements.quizCard.classList.add("answer-correct");
            this.showQuizFeedback(true, isField ? "정답이에요! 월드 상태가 좋아졌어요." : "정답이에요! 초능력 조각이 빛났어요.");

            if (this.getCurrentStage().boss && !isField) {
                this.dungeon.bossHp = Math.max(0, this.dungeon.bossHp - 1);
                this.updateBossHp();
            }

            this.updateHud();
            this.saveProgress();
            setTimeout(() => isField ? this.completeFieldChallenge(true) : this.advanceQuestion(), 720);
            return;
        }

        optionButtons[this.selectedAnswer].classList.add("wrong");
        this.elements.quizCard.classList.add("answer-wrong");
        this.showQuizFeedback(false, isField ? "오답이에요. 힌트를 다시 찾아야 해요." : "오답이에요. 오염도가 조금 올라갔어요.");
        if (!isField) {
            this.applyFailurePenalty("던전 오답");
        }
        this.updateHud();
        this.saveProgress();

        if (isField) {
            setTimeout(() => this.completeFieldChallenge(false), 720);
            return;
        }

        setTimeout(() => {
            this.elements.quizCard.classList.remove("answer-wrong");
            optionButtons[this.selectedAnswer]?.classList.remove("wrong", "selected");
            this.selectedAnswer = null;
            this.answerLocked = false;
        }, 520);
    },

    completeFieldChallenge(isCorrect) {
        this.clearTimer();
        const callbacks = this.activeFieldChallenge;
        this.activeFieldChallenge = null;
        this.dungeon = null;
        this.showScreen("worldScreen");
        if (isCorrect) {
            callbacks?.onCorrect?.();
        } else {
            callbacks?.onWrong?.();
        }
        this.checkPortalActivation();
        this.renderWorld();
        this.updateQuest();
        this.updateHud();
        this.saveProgress();
    },

    failDungeon() {
        this.clearTimer();
        if (this.dungeon?.mode === "field") {
            this.showQuizFeedback(false, "시간이 끝났어요. 힌트를 다시 찾아야 해요.");
            setTimeout(() => this.completeFieldChallenge(false), 900);
            return;
        }

        this.showQuizFeedback(false, "시간이 끝났어요. 마을로 돌아가 다시 도전해요.");
        this.answerLocked = true;
        setTimeout(() => {
            const stage = this.getCurrentStage();
            this.player = { x: Math.max(1, stage.portal.x - 1), y: stage.portal.y };
            this.showScreen("worldScreen");
            this.renderWorld();
            this.updateQuest();
            this.saveProgress();
        }, 1700);
    },

    checkPortalEntry() {
        const stage = this.getCurrentStage();
        const isOnPortal = this.player.x === stage.portal.x && this.player.y === stage.portal.y;
        if (!isOnPortal || this.isTransitioning) return;

        if (!this.unlockPortalIfReady(true)) {
            this.showPortalStatus();
            return;
        }

        this.enterPortal();
    },

    showPortalStatus() {
        if (this.unlockPortalIfReady(true)) {
            this.showTemporaryPrompt("포탈이 열렸어요. 던전에 들어갑니다!");
            setTimeout(() => {
                if (!this.isTransitioning && this.currentScreen === "worldScreen") this.enterPortal();
            }, 450);
            return;
        }

        const missing = this.getMissingRequirementText();
        this.showDialogue("잠긴 포탈", `아직 포탈이 열리지 않았어요.\n${missing.join("\n")}`, "portal");
    },

    handlePortal() {
        if (!this.unlockPortalIfReady(true)) {
            this.showPortalStatus();
            return;
        }

        this.enterPortal();
    },

    checkPortalActivation() {
        this.unlockPortalIfReady(true);
    },

    unlockPortalIfReady(announce = false) {
        const state = this.getStageState();
        if (state.portalOpen) return true;
        if (!this.arePortalRequirementsMet()) return false;

        state.portalOpen = true;
        state.portalAnnounced = true;
        if (announce) {
            this.elements.worldMap.classList.add("portal-opened");
            this.showTemporaryPrompt("조건 완료! 포탈이 열렸어요.");
            setTimeout(() => this.elements.worldMap?.classList.remove("portal-opened"), 1300);
        }
        return true;
    },

    arePortalRequirementsMet() {
        return this.getMissingRequirementText().length === 0;
    },

    getMissingRequirementText() {
        const scenario = this.getScenario();
        const state = this.getStageState();
        const req = scenario?.requirements || {};
        const missing = [];

        if (req.npcs && state.talkedNpcIds.length < req.npcs) missing.push(`대화 단서 ${state.talkedNpcIds.length}/${req.npcs}`);
        if (req.signs && state.readSignIds.length < req.signs) missing.push(`표지판 단서 ${state.readSignIds.length}/${req.signs}`);
        if (req.fragments && state.fragments < req.fragments) missing.push(`과학 조각 ${state.fragments}/${req.fragments}`);
        if (req.monsters && state.solvedMonsterIds.length < req.monsters) missing.push(`몬스터 해결 ${state.solvedMonsterIds.length}/${req.monsters}`);
        if (req.pollution && state.clearedPollutionIds.length < req.pollution) missing.push(`오염 블록 정화 ${state.clearedPollutionIds.length}/${req.pollution}`);
        if (req.bridge && !state.bridgeFixed) missing.push("다리 복구 0/1");
        if (req.buttonPuzzle && !state.buttonPuzzleSolved) missing.push("버튼 퍼즐 0/1");

        return missing;
    },

    applyFailurePenalty(message) {
        this.score = Math.max(0, this.score - 10);
        const state = this.getStageState();
        state.needsHint = true;
        this.showTemporaryPrompt(message);
    },

    updateQuest() {
        if (this.currentScreen === "dungeonScreen" && this.dungeon) {
            const stage = this.getCurrentStage();
            if (this.dungeon.mode === "field") {
                this.elements.questText.textContent = "현장 문제를 해결해 월드 상태 바꾸기";
            } else if (stage.boss) {
                this.elements.questText.textContent = "제한시간 안에 최종 시험 해결하기";
            } else {
                this.elements.questText.textContent = `최종 시험 ${this.dungeon.currentIndex + 1}/${this.dungeon.questions.length} 해결하기`;
            }
            return;
        }

        const stage = this.getCurrentStage();
        const state = this.getStageState();
        const req = this.getScenario()?.requirements || {};
        const portalReady = state.portalOpen || this.arePortalRequirementsMet();
        this.elements.worldStageTitle.textContent = `스테이지 ${stage.id}: ${stage.name}`;

        const items = [
            { done: !req.npcs || state.talkedNpcIds.length >= req.npcs, text: `대화 단서 ${state.talkedNpcIds.length}/${req.npcs || 0}` },
            { done: !req.signs || state.readSignIds.length >= req.signs, text: `표지판 단서 ${state.readSignIds.length}/${req.signs || 0}` },
            { done: !req.fragments || state.fragments >= req.fragments, text: `과학 조각 ${state.fragments}/${req.fragments || 0}` },
            { done: !req.monsters || state.solvedMonsterIds.length >= req.monsters, text: `몬스터 해결 ${state.solvedMonsterIds.length}/${req.monsters || 0}` },
            { done: !req.pollution || state.clearedPollutionIds.length >= req.pollution, text: `오염 정화 ${state.clearedPollutionIds.length}/${req.pollution || 0}` }
        ];
        if (req.bridge) items.push({ done: state.bridgeFixed, text: `다리 복구 ${state.bridgeFixed ? 1 : 0}/1` });
        if (req.buttonPuzzle) items.push({ done: state.buttonPuzzleSolved, text: `버튼 퍼즐 ${state.buttonPuzzleSolved ? 1 : 0}/1` });
        items.push({ done: portalReady, text: portalReady ? "포탈 열림" : "포탈 잠김" });

        this.elements.questText.innerHTML = `<ul class="quest-checklist">${items.map((item) => (
            `<li class="${item.done ? "done" : ""}">${item.text}</li>`
        )).join("")}</ul>`;

        if (this.elements.worldInventory) {
            this.elements.worldInventory.textContent = `조각 ${state.fragments}개 · 열쇠 ${state.keys}개`;
        }
    }
});

const blockRenderer3D = {
    canvas: null,
    ctx: null,
    raf: null,
    needsResize: true,
    renderRadius: 14,

    ensure(gameInstance) {
        const canvas = gameInstance.elements.worldCanvas || document.getElementById("worldCanvas");
        if (!canvas) return false;
        if (this.canvas !== canvas) {
            this.canvas = canvas;
            this.ctx = canvas.getContext("2d");
            this.needsResize = true;
        }
        return Boolean(this.ctx);
    },

    targetFps: 30,
    lastFrameTime: 0,

    start(gameInstance) {
        if (!this.ensure(gameInstance)) return;
        if (this.raf) cancelAnimationFrame(this.raf);
        const frameInterval = 1000 / this.targetFps;
        this.lastFrameTime = 0;
        const loop = (time) => {
            if (gameInstance.currentScreen === "worldScreen") {
                const elapsed = time - this.lastFrameTime;
                if (elapsed >= frameInterval) {
                    this.lastFrameTime = time - (elapsed % frameInterval);
                    this.render(gameInstance, time);
                }
                this.raf = requestAnimationFrame(loop);
            } else {
                this.raf = null;
            }
        };
        this.raf = requestAnimationFrame(loop);
    },

    requestRender(gameInstance) {
        if (gameInstance.currentScreen === "worldScreen") {
            this.render(gameInstance, performance.now());
        }
    },

    resize() {
        if (!this.canvas) return;
        const rect = this.canvas.getBoundingClientRect();
        const scale = Math.max(1, Math.min(2, window.devicePixelRatio || 1));
        const nextWidth = Math.max(320, Math.floor(rect.width * scale));
        const nextHeight = Math.max(220, Math.floor(rect.height * scale));
        if (this.canvas.width !== nextWidth || this.canvas.height !== nextHeight) {
            this.canvas.width = nextWidth;
            this.canvas.height = nextHeight;
            this.needsResize = false;
        }
    },

    render(gameInstance, time = 0) {
        if (!this.ensure(gameInstance)) return;
        this.resize();

        const ctx = this.ctx;
        const width = this.canvas.width;
        const height = this.canvas.height;
        const stage = gameInstance.getCurrentStage();
        const state = gameInstance.getStageState();
        const theme = gameInstance.getStageTheme?.() || stageThemes[stage.id] || stageThemes[1];
        this.centerX = (gameInstance.cols - 1) / 2;
        this.centerY = (gameInstance.rows - 1) / 2;
        const playerPosition = gameInstance.getPlayerRenderPosition?.() || gameInstance.player;
        const playerWorld = this.gridToWorld(playerPosition.x, playerPosition.y);
        const forward = gameInstance.getLookVector3D
            ? gameInstance.getLookVector3D()
            : this.facingVector(gameInstance.facing || "right");
        const camera = {
            x: playerWorld.x,
            y: 1.08,
            z: playerWorld.z
        };
        const target = {
            x: camera.x + forward.x * 5,
            y: camera.y + (forward.y ?? -0.036) * 5,
            z: camera.z + forward.z * 5
        };
        const basis = this.makeCameraBasis(camera, target);
        const view = { camera, basis, width, height, focal: Math.min(width, height) * 0.95 };

        const sky = ctx.createLinearGradient(0, 0, 0, height);
        sky.addColorStop(0, theme.skyTop);
        sky.addColorStop(0.47, theme.skyMid);
        sky.addColorStop(0.5, theme.horizon);
        sky.addColorStop(1, theme.ground);
        ctx.fillStyle = sky;
        ctx.fillRect(0, 0, width, height);

        if (theme.bloodMoon) {
            this.drawBloodMoon(ctx, width, height, time);
        }

        const faces = [];
        const labels = [];
        this.collectTerrain(gameInstance, stage, state, faces, playerPosition);
        this.collectEntities(gameInstance, faces, labels, time);
        this.refreshDepth(faces, labels);

        faces.sort((a, b) => b.depth - a.depth);
        faces.forEach((face) => this.drawFace(ctx, face));

        labels.sort((a, b) => b.depth - a.depth);
        labels.forEach((label) => this.drawLabel(ctx, label, view));

        this.drawFirstPersonHands(ctx, width, height, time);
        this.drawVignette(ctx, width, height, theme);
        gameInstance.renderMiniMap?.();
    },

    collectTerrain(gameInstance, stage, state, faces, playerPosition) {
        const radius = this.renderRadius;
        const minX = Math.max(0, Math.floor(playerPosition.x - radius));
        const maxX = Math.min(gameInstance.cols - 1, Math.ceil(playerPosition.x + radius));
        const minY = Math.max(0, Math.floor(playerPosition.y - radius));
        const maxY = Math.min(gameInstance.rows - 1, Math.ceil(playerPosition.y + radius));

        for (let y = minY; y <= maxY; y += 1) {
            for (let x = minX; x <= maxX; x += 1) {
                const tile = stage.map[y]?.[x] || "G";
                const mined = gameInstance.isMinedTile?.(x, y);
                const visualTile = mined && ["T", "R"].includes(tile) ? "G" : tile;
                const color = this.tileColor(visualTile, gameInstance.completedStages.length, x, y);
                const pos = this.gridToWorld(x, y);
                this.addCuboid(faces, pos.x, -0.16, pos.z, 1, 0.22, 1, color);

                if (!mined && tile === "T") {
                    this.addBlockTree(faces, pos.x, pos.z);
                } else if (!mined && tile === "R") {
                    this.addCuboid(faces, pos.x, 0, pos.z, 0.72, 0.55, 0.72, "#858a8e");
                } else if (tile === "W") {
                    this.addCuboid(faces, pos.x, -0.08, pos.z, 0.92, 0.08, 0.92, "#2f9fca");
                } else if (tile === "C") {
                    this.addCuboid(faces, pos.x, 0.02, pos.z, 0.72, 0.22, 0.72, "#6b3a7a");
                } else if (tile === "F") {
                    this.addCuboid(faces, pos.x - 0.18, 0.08, pos.z, 0.12, 0.35, 0.12, "#ff8ba8");
                    this.addCuboid(faces, pos.x + 0.18, 0.08, pos.z - 0.08, 0.12, 0.32, 0.12, "#f3d657");
                }
            }
        }

        const scenario = gameInstance.getScenario(stage);
        if (scenario?.bridge && state.bridgeFixed) {
            const bridge = this.gridToWorld(scenario.bridge.x, scenario.bridge.y);
            this.addCuboid(faces, bridge.x, 0.02, bridge.z, 1, 0.18, 1, "#9a673a");
        }

        this.collectVillageBuildings(gameInstance, stage, faces, playerPosition);
    },

    addBlockTree(faces, x, z) {
        this.addCuboid(faces, x, 0.02, z, 0.26, 1.45, 0.26, "#5b351d");
        this.addCuboid(faces, x, 1.06, z, 0.9, 0.72, 0.9, "#236d32");
        this.addCuboid(faces, x, 1.58, z, 0.72, 0.62, 0.72, "#2e8739");
        this.addCuboid(faces, x, 2.04, z, 0.46, 0.42, 0.46, "#3a9b45");
    },

    collectVillageBuildings(gameInstance, stage, faces, playerPosition) {
        const clean = gameInstance.completedStages.length;
        const houses = [
            { x: 2.1, y: 2.2, roof: clean >= 2 ? "#8b4513" : "#5c3f35", wall: clean >= 2 ? "#d2b48c" : "#9d806a" },
            { x: 16.2, y: 2.2, roof: stage.id >= 4 ? "#59416a" : "#8b4513", wall: "#c9a36f" },
            { x: 17.2, y: 11.0, roof: clean >= 4 ? "#9b5b2f" : "#604238", wall: clean >= 4 ? "#d7bd82" : "#8e7767" },
            { x: 38.5, y: 17.6, roof: "#6b4c2e", wall: "#b69262" },
            { x: 55.2, y: 35.8, roof: "#4d3a5f", wall: "#9d8b72" },
            { x: 76.5, y: 52.2, roof: "#7a342f", wall: "#a88365" }
        ];

        houses.forEach((house, index) => {
            if (this.isNearGrid(playerPosition, house.x, house.y, this.renderRadius + 6)) {
                this.addHouse(faces, house.x, house.y, house, index);
            }
        });
    },

    isNearGrid(playerPosition, x, y, radius) {
        return Math.abs(playerPosition.x - x) <= radius && Math.abs(playerPosition.y - y) <= radius;
    },

    addHouse(faces, gridX, gridY, house, index) {
        const pos = this.gridToWorld(gridX, gridY);
        const width = index === 1 ? 1.85 : 1.65;
        const depth = index === 2 ? 1.55 : 1.7;
        this.addCuboid(faces, pos.x, 0, pos.z, width, 0.24, depth, "#777777");
        this.addCuboid(faces, pos.x, 0.24, pos.z, width, 0.92, depth, house.wall);
        this.addCuboid(faces, pos.x, 1.14, pos.z, width + 0.18, 0.34, depth + 0.18, house.roof);
        this.addCuboid(faces, pos.x, 1.44, pos.z, width * 0.76, 0.28, depth * 0.76, house.roof);
        this.addCuboid(faces, pos.x, 0.28, pos.z + depth / 2 + 0.02, 0.36, 0.56, 0.08, "#5b351d");
        this.addCuboid(faces, pos.x - width * 0.26, 0.68, pos.z + depth / 2 + 0.03, 0.22, 0.22, 0.06, "#aee6ff");
        this.addCuboid(faces, pos.x + width * 0.26, 0.68, pos.z + depth / 2 + 0.03, 0.22, 0.22, 0.06, "#aee6ff");
    },

    collectEntities(gameInstance, faces, labels, time) {
        const pulse = Math.sin(time / 520) * 0.035;
        const playerPosition = gameInstance.getPlayerRenderPosition?.() || gameInstance.player;
        gameInstance.getVisibleEntities().forEach((entity) => {
            const pos = this.gridToWorld(entity.x, entity.y);
            const style = this.entityStyle(entity);
            const lift = style.float ? pulse + 0.16 : 0;

            if (style.model === "villager") {
                this.addVillager(faces, pos.x, lift, pos.z, style);
            } else if (style.model === "golem") {
                this.addGolem(faces, pos.x, lift, pos.z, style);
            } else if (style.model === "portal") {
                this.addPortalFrame(faces, pos.x, lift, pos.z, style);
            } else if (style.model === "slime") {
                this.addSlime(faces, pos.x, lift, pos.z, style);
            } else {
                this.addCuboid(faces, pos.x, lift, pos.z, style.w, style.h, style.d, style.color);

                if (style.head) {
                    this.addCuboid(faces, pos.x, lift + style.h, pos.z, style.w * 0.72, style.h * 0.38, style.d * 0.72, style.head);
                }
            }

            const distanceToPlayer = Math.hypot(entity.x - playerPosition.x, entity.y - playerPosition.y);
            if (distanceToPlayer <= 5.2 || entity.kind === "portal") {
                labels.push({
                    text: entity.label || entity.name,
                    point: { x: pos.x, y: lift + style.h + 0.48, z: pos.z },
                    color: style.labelColor || "#fff7cf"
                });
            }
        });

    },

    tileColor(tile, cleanLevel, x, y) {
        if (tile === "C" && cleanLevel > ((x + y) % 6)) return "#79c95a";
        if (tile === "G" && cleanLevel >= 3 && (x * 2 + y) % 11 === 0) return "#72c95a";
        return {
            G: "#5aad47",
            P: "#c59a5f",
            T: "#4d933f",
            R: "#7f8589",
            W: "#36a5c9",
            C: "#6c3d7d",
            F: "#6fc85a",
            D: "#77604c"
        }[tile] || "#5aad47";
    },

    entityStyle(entity) {
        const classes = entity.className || "";
        if (entity.kind === "portal") {
            const locked = classes.includes("locked");
            return {
                color: locked ? "#5e5a66" : "#8f55ff",
                d: 0.34,
                h: locked ? 1.15 : 1.9,
                labelColor: locked ? "#d8d4df" : "#f5d8ff",
                model: "portal",
                w: 0.82
            };
        }
        if (classes.includes("entity-fragment")) return { color: "#74e0ff", d: 0.42, float: true, h: 0.42, w: 0.42 };
        if (classes.includes("entity-emerald")) return { color: "#23c983", d: 0.42, float: true, h: 0.42, w: 0.42 };
        if (classes.includes("entity-key")) return { color: "#f2c94c", d: 0.48, float: true, h: 0.32, w: 0.66 };
        if (classes.includes("entity-sign")) return { color: "#c99558", d: 0.24, h: 0.85, w: 0.72 };
        if (classes.includes("entity-pollution")) return { color: "#6b3a7a", d: 0.78, h: 0.72, w: 0.78 };
        if (classes.includes("entity-button")) return { color: "#f2c94c", d: 0.58, h: 0.22, w: 0.58 };
        if (classes.includes("entity-bridge")) return { color: "#9a673a", d: 0.92, h: 0.24, w: 0.92 };
        if (classes.includes("entity-warning")) return { color: "#e3c743", d: 0.46, h: 0.78, w: 0.46 };
        if (classes.includes("entity-golem")) return { color: "#c6c2b5", d: 0.72, h: 1.45, head: "#9da19e", model: "golem", w: 0.9 };
        if (classes.includes("entity-boss")) return { color: "#9c594e", d: 0.82, h: 1.15, head: "#713b35", model: "golem", w: 0.9 };
        if (classes.includes("entity-monster")) return { color: classes.includes("entity-base") ? "#54c6c9" : "#58c66a", d: 0.7, float: true, h: 0.68, model: "slime", w: 0.7 };
        if (classes.includes("entity-feast")) return { color: "#bc5f37", d: 0.75, h: 0.38, w: 0.9 };
        return { color: "#8b5a2b", d: 0.52, h: 1.45, head: "#ffcc99", model: "villager", w: 0.58 };
    },

    addVillager(faces, x, y, z, style) {
        const face = this.faceTowardCamera(x, z);
        this.addCuboid(faces, x, y + 0.02, z, 0.62, 1.02, 0.5, style.color || "#8b5a2b");
        this.addCuboid(faces, x, y + 1.08, z, 0.56, 0.5, 0.56, style.head || "#ffcc99");
        this.addFacingDetail(faces, x, y + 0.76, z, 0.66, 0.54, 0, 0.78, 0.86, 0.16, 0.08, "#6d4420", face);
        this.addFacingDetail(faces, x, y + 1.08, z, 0.56, 0.56, -0.13, 1.39, 0.06, 0.07, 0.045, "#21170f", face);
        this.addFacingDetail(faces, x, y + 1.08, z, 0.56, 0.56, 0.13, 1.39, 0.06, 0.07, 0.045, "#21170f", face);
        this.addFacingDetail(faces, x, y + 1.08, z, 0.56, 0.56, 0, 1.26, 0.13, 0.18, 0.12, "#e0a080", face);
        this.addFacingDetail(faces, x, y + 1.08, z, 0.56, 0.56, 0, 1.16, 0.22, 0.04, 0.045, "#21170f", face);
    },

    addGolem(faces, x, y, z, style) {
        const face = this.faceTowardCamera(x, z);
        this.addCuboid(faces, x, y, z, 0.92, 1.22, 0.64, style.color || "#c6c2b5");
        this.addCuboid(faces, x, y + 1.18, z, 0.74, 0.58, 0.72, style.head || "#9da19e");
        this.addCuboid(faces, x - 0.62, y + 0.16, z, 0.24, 0.92, 0.26, "#8e918f");
        this.addCuboid(faces, x + 0.62, y + 0.16, z, 0.24, 0.92, 0.26, "#8e918f");
        this.addFacingDetail(faces, x, y + 1.18, z, 0.74, 0.72, -0.14, 1.48, 0.08, 0.08, 0.05, "#111111", face);
        this.addFacingDetail(faces, x, y + 1.18, z, 0.74, 0.72, 0.14, 1.48, 0.08, 0.08, 0.05, "#111111", face);
        this.addFacingDetail(faces, x, y + 1.18, z, 0.74, 0.72, 0, 1.32, 0.18, 0.2, 0.12, "#c98972", face);
    },

    addPortalFrame(faces, x, y, z, style) {
        const locked = style.color === "#5e5a66";
        const frame = locked ? "#45424d" : "#432566";
        const core = locked ? "#686472" : "#a66cff";
        this.addCuboid(faces, x - 0.38, y, z, 0.18, 1.82, 0.18, frame);
        this.addCuboid(faces, x + 0.38, y, z, 0.18, 1.82, 0.18, frame);
        this.addCuboid(faces, x, y + 1.66, z, 0.92, 0.18, 0.18, frame);
        this.addCuboid(faces, x, y + 0.72, z, 0.54, 1.28, 0.08, core);
        if (!locked) {
            this.addCuboid(faces, x, y + 0.82, z, 0.34, 0.94, 0.1, "#d9a8ff");
        }
    },

    addSlime(faces, x, y, z, style) {
        const face = this.faceTowardCamera(x, z);
        this.addCuboid(faces, x, y, z, 0.76, 0.56, 0.76, style.color || "#58c66a");
        this.addCuboid(faces, x, y + 0.45, z, 0.54, 0.24, 0.54, this.shade(style.color || "#58c66a", 24));
        this.addFacingDetail(faces, x, y, z, 0.76, 0.76, -0.16, y + 0.36, 0.09, 0.09, 0.05, "#163318", face);
        this.addFacingDetail(faces, x, y, z, 0.76, 0.76, 0.16, y + 0.36, 0.09, 0.09, 0.05, "#163318", face);
        this.addFacingDetail(faces, x, y, z, 0.76, 0.76, 0, y + 0.22, 0.28, 0.06, 0.05, "#163318", face);
    },

    faceTowardCamera(x, z) {
        const camera = this.view?.camera || { x: 0, z: 0 };
        const dx = camera.x - x;
        const dz = camera.z - z;
        if (Math.abs(dx) > Math.abs(dz)) return { axis: "x", dir: Math.sign(dx) || 1 };
        return { axis: "z", dir: Math.sign(dz) || 1 };
    },

    addFacingDetail(faces, x, baseY, z, baseW, baseD, offset, y, w, h, thickness, color, face) {
        if (face.axis === "x") {
            this.addCuboid(faces, x + face.dir * (baseW / 2 + thickness / 2 + 0.012), y, z + offset, thickness, h, w, color);
            return;
        }
        this.addCuboid(faces, x + offset, y, z + face.dir * (baseD / 2 + thickness / 2 + 0.012), w, h, thickness, color);
    },

    addCuboid(faces, x, y, z, w, h, d, color) {
        const x0 = x - w / 2;
        const x1 = x + w / 2;
        const y0 = y;
        const y1 = y + h;
        const z0 = z - d / 2;
        const z1 = z + d / 2;
        const vertices = [
            { x: x0, y: y0, z: z0 }, { x: x1, y: y0, z: z0 },
            { x: x1, y: y1, z: z0 }, { x: x0, y: y1, z: z0 },
            { x: x0, y: y0, z: z1 }, { x: x1, y: y0, z: z1 },
            { x: x1, y: y1, z: z1 }, { x: x0, y: y1, z: z1 }
        ];
        [
            { ids: [0, 1, 2, 3], shade: -18 },
            { ids: [5, 4, 7, 6], shade: -8 },
            { ids: [4, 0, 3, 7], shade: -12 },
            { ids: [1, 5, 6, 2], shade: -22 },
            { ids: [3, 2, 6, 7], shade: 14 },
            { ids: [4, 5, 1, 0], shade: -28 }
        ].forEach((face) => {
            faces.push({
                color: this.shade(color, face.shade),
                points: face.ids.map((id) => vertices[id]),
                depth: face.ids.reduce((sum, id) => sum + vertices[id].z + vertices[id].x * 0.05 + vertices[id].y * 0.03, 0) / face.ids.length
            });
        });
    },

    drawFace(ctx, face) {
        const projected = face.points.map((point) => this.project(point)).filter(Boolean);
        if (projected.length !== face.points.length) return;

        ctx.beginPath();
        projected.forEach((point, index) => {
            if (index === 0) ctx.moveTo(point.x, point.y);
            else ctx.lineTo(point.x, point.y);
        });
        ctx.closePath();
        ctx.fillStyle = face.color;
        ctx.fill();
        ctx.strokeStyle = "rgba(20, 12, 7, 0.22)";
        ctx.lineWidth = 1;
        ctx.stroke();
    },

    drawLabel(ctx, label) {
        const point = this.project(label.point);
        if (!point) return;
        ctx.font = `${Math.max(14, Math.round(this.canvas.height / 48))}px Courier New, monospace`;
        const metrics = ctx.measureText(label.text);
        const pad = 6;
        ctx.fillStyle = "rgba(32, 22, 15, 0.82)";
        ctx.fillRect(point.x - metrics.width / 2 - pad, point.y - 24, metrics.width + pad * 2, 22);
        ctx.fillStyle = label.color;
        ctx.textAlign = "center";
        ctx.fillText(label.text, point.x, point.y - 8);
    },

    drawFirstPersonHands(ctx, width, height, time) {
        const bob = Math.sin(time / 260) * height * 0.004;
        const rightX = width * 0.67;
        const rightY = height * 0.72 + bob;
        const armWidth = width * 0.13;
        const armHeight = height * 0.2;

        ctx.save();
        ctx.imageSmoothingEnabled = false;
        ctx.fillStyle = "#2f80ed";
        ctx.fillRect(rightX, rightY, armWidth, armHeight);
        ctx.fillStyle = "#1e5eb7";
        ctx.fillRect(rightX, rightY + armHeight * 0.68, armWidth, armHeight * 0.16);
        ctx.fillStyle = "#f2c29b";
        ctx.fillRect(rightX + armWidth * 0.06, rightY - armHeight * 0.18, armWidth * 0.88, armHeight * 0.26);
        ctx.fillStyle = "#7b4a2c";
        ctx.fillRect(rightX + armWidth * 0.73, rightY - armHeight * 0.44, armWidth * 0.12, armHeight * 0.42);
        ctx.fillStyle = "#9fa7ad";
        ctx.fillRect(rightX + armWidth * 0.61, rightY - armHeight * 0.5, armWidth * 0.36, armHeight * 0.12);

        const leftX = width * 0.18;
        const leftY = height * 0.78 - bob * 0.4;
        ctx.fillStyle = "#2f80ed";
        ctx.fillRect(leftX, leftY, armWidth * 0.72, armHeight * 0.58);
        ctx.fillStyle = "#f2c29b";
        ctx.fillRect(leftX + armWidth * 0.04, leftY - armHeight * 0.15, armWidth * 0.58, armHeight * 0.22);
        ctx.restore();
    },

    drawBloodMoon(ctx, width, height, time) {
        if (!this.view) return;

        const cam = this.view.camera;
        const moonDir = this.normalize({ x: -0.4, y: 0.7, z: -0.6 });
        const farDist = 500;
        const moonWorld = {
            x: cam.x + moonDir.x * farDist,
            y: cam.y + moonDir.y * farDist,
            z: cam.z + moonDir.z * farDist
        };

        const projected = this.project(moonWorld);
        if (!projected) return;

        const moonX = projected.x;
        const moonY = projected.y;
        const moonRadius = Math.min(width, height) * 0.07;

        ctx.save();

        const glowRadius = moonRadius * 3.5;
        const outerGlow = ctx.createRadialGradient(moonX, moonY, moonRadius * 0.5, moonX, moonY, glowRadius);
        outerGlow.addColorStop(0, "rgba(180, 30, 20, 0.25)");
        outerGlow.addColorStop(0.3, "rgba(140, 20, 15, 0.12)");
        outerGlow.addColorStop(0.6, "rgba(100, 10, 10, 0.05)");
        outerGlow.addColorStop(1, "rgba(60, 5, 5, 0)");
        ctx.fillStyle = outerGlow;
        ctx.fillRect(moonX - glowRadius, moonY - glowRadius, glowRadius * 2, glowRadius * 2);

        const haloGrad = ctx.createRadialGradient(moonX, moonY, moonRadius * 0.9, moonX, moonY, moonRadius * 1.6);
        haloGrad.addColorStop(0, "rgba(200, 50, 30, 0.35)");
        haloGrad.addColorStop(0.5, "rgba(160, 30, 20, 0.12)");
        haloGrad.addColorStop(1, "rgba(120, 15, 10, 0)");
        ctx.beginPath();
        ctx.arc(moonX, moonY, moonRadius * 1.6, 0, Math.PI * 2);
        ctx.fillStyle = haloGrad;
        ctx.fill();

        const moonGrad = ctx.createRadialGradient(
            moonX - moonRadius * 0.25, moonY - moonRadius * 0.2, moonRadius * 0.1,
            moonX, moonY, moonRadius
        );
        moonGrad.addColorStop(0, "#d44030");
        moonGrad.addColorStop(0.45, "#b82a20");
        moonGrad.addColorStop(0.8, "#8a1a12");
        moonGrad.addColorStop(1, "#5a0e08");
        ctx.beginPath();
        ctx.arc(moonX, moonY, moonRadius, 0, Math.PI * 2);
        ctx.fillStyle = moonGrad;
        ctx.fill();

        const craters = [
            { ox: -0.3, oy: -0.15, r: 0.18 },
            { ox: 0.2, oy: 0.25, r: 0.14 },
            { ox: -0.1, oy: 0.35, r: 0.1 },
            { ox: 0.35, oy: -0.1, r: 0.12 },
            { ox: 0.05, oy: -0.35, r: 0.09 },
            { ox: -0.35, oy: 0.2, r: 0.08 }
        ];
        craters.forEach((crater) => {
            const cx = moonX + crater.ox * moonRadius;
            const cy = moonY + crater.oy * moonRadius;
            const cr = crater.r * moonRadius;
            ctx.beginPath();
            ctx.arc(cx, cy, cr, 0, Math.PI * 2);
            ctx.fillStyle = "rgba(60, 8, 5, 0.35)";
            ctx.fill();
        });

        const pulse = Math.sin(time / 1800) * 0.08 + 0.92;
        const edgeGlow = ctx.createRadialGradient(moonX, moonY, moonRadius * 0.7, moonX, moonY, moonRadius * 1.05);
        edgeGlow.addColorStop(0, "rgba(255, 80, 40, 0)");
        edgeGlow.addColorStop(0.85, `rgba(255, 60, 30, ${0.2 * pulse})`);
        edgeGlow.addColorStop(1, `rgba(200, 30, 15, ${0.35 * pulse})`);
        ctx.beginPath();
        ctx.arc(moonX, moonY, moonRadius * 1.05, 0, Math.PI * 2);
        ctx.fillStyle = edgeGlow;
        ctx.fill();

        ctx.restore();
    },


    drawVignette(ctx, width, height, theme = stageThemes[1]) {
        const gradient = ctx.createRadialGradient(width / 2, height / 2, height * 0.15, width / 2, height / 2, height * 0.78);
        gradient.addColorStop(0, "rgba(0,0,0,0)");
        gradient.addColorStop(1, `rgba(0,0,0,${theme.vignette ?? 0.28})`);
        ctx.fillStyle = gradient;
        ctx.fillRect(0, 0, width, height);
    },

    gridToWorld(x, y) {
        return { x: x - (this.centerX ?? 7), z: y - (this.centerY ?? 5) };
    },

    facingVector(facing) {
        return {
            up: { x: 0, z: -1 },
            right: { x: 1, z: 0 },
            down: { x: 0, z: 1 },
            left: { x: -1, z: 0 }
        }[facing] || { x: 1, z: 0 };
    },

    lookVectorToWorld(vector) {
        return { x: vector.x, z: vector.y };
    },

    makeCameraBasis(camera, target) {
        const forward = this.normalize({ x: target.x - camera.x, y: target.y - camera.y, z: target.z - camera.z });
        const right = this.normalize(this.cross(forward, { x: 0, y: 1, z: 0 }));
        const up = this.cross(right, forward);
        this.view = { camera, forward, right, up };
        return this.view;
    },

    refreshDepth(faces, labels) {
        faces.forEach((face) => {
            face.depth = face.points.reduce((sum, point) => sum + this.depth(point), 0) / face.points.length;
        });
        labels.forEach((label) => {
            label.depth = this.depth(label.point);
        });
    },

    depth(point) {
        if (!this.view) return 0;
        const rel = {
            x: point.x - this.view.camera.x,
            y: point.y - this.view.camera.y,
            z: point.z - this.view.camera.z
        };
        return this.dot(rel, this.view.forward);
    },

    project(point) {
        if (!this.view || !this.canvas) return null;
        const rel = {
            x: point.x - this.view.camera.x,
            y: point.y - this.view.camera.y,
            z: point.z - this.view.camera.z
        };
        const x = this.dot(rel, this.view.right);
        const y = this.dot(rel, this.view.up);
        const z = this.dot(rel, this.view.forward);
        if (z <= 0.2) return null;
        const focal = Math.min(this.canvas.width, this.canvas.height) * 0.95;
        return {
            x: this.canvas.width / 2 + (x / z) * focal,
            y: this.canvas.height / 2 - (y / z) * focal,
            depth: z
        };
    },

    normalize(v) {
        const length = Math.hypot(v.x, v.y, v.z) || 1;
        return { x: v.x / length, y: v.y / length, z: v.z / length };
    },

    cross(a, b) {
        return {
            x: a.y * b.z - a.z * b.y,
            y: a.z * b.x - a.x * b.z,
            z: a.x * b.y - a.y * b.x
        };
    },

    dot(a, b) {
        return a.x * b.x + a.y * b.y + a.z * b.z;
    },

    shade(hex, amount) {
        const value = hex.replace("#", "");
        const r = Math.max(0, Math.min(255, parseInt(value.slice(0, 2), 16) + amount));
        const g = Math.max(0, Math.min(255, parseInt(value.slice(2, 4), 16) + amount));
        const b = Math.max(0, Math.min(255, parseInt(value.slice(4, 6), 16) + amount));
        return `rgb(${r}, ${g}, ${b})`;
    }
};

const cacheElementsBefore3D = game.cacheElements.bind(game);
const resetRuntimeBefore3D = game.resetRuntimeState.bind(game);
const enterStageBefore3D = game.enterStage.bind(game);
const startGameBefore3D = game.startGame.bind(game);
const continueGameBefore3D = game.continueGame.bind(game);
const restartBefore3D = game.restart.bind(game);
const renderWorldBefore3D = game.renderWorld.bind(game);
const renderPlayerPositionBefore3D = game.renderPlayerPosition.bind(game);
const movePlayerBefore3D = game.movePlayer.bind(game);
const updateHudBefore3D = game.updateHud.bind(game);
const applySavedGameBefore3D = game.applySavedGame.bind(game);

Object.assign(game, {
    playerName: "손님",
    facing: "right",
    musicContext: null,
    musicTimer: null,
    musicStageId: null,

    cacheElements() {
        cacheElementsBefore3D();
        this.elements.nicknameInput = document.getElementById("nicknameInput");
        this.elements.playerNameBadge = document.getElementById("playerNameBadge");
        this.elements.worldCanvas = document.getElementById("worldCanvas");
        this.elements.worldViewLabel = document.getElementById("worldViewLabel");
        this.elements.miniMapCanvas = document.getElementById("miniMapCanvas");
        this.elements.miniMapCoords = document.getElementById("miniMapCoords");
    },

    resetRuntimeState() {
        resetRuntimeBefore3D();
        this.facing = "right";
    },

    startGame() {
        this.playerName = this.normalizePlayerName(this.elements.nicknameInput?.value);
        startGameBefore3D();
        this.startStageMusic();
        this.updateHud();
    },

    continueGame() {
        continueGameBefore3D();
        this.startStageMusic();
    },

    restart() {
        this.stopStageMusic();
        restartBefore3D();
    },

    enterStage(index, resumed = false) {
        enterStageBefore3D(index, resumed);
        this.applyStageTheme();
        this.startStageMusic();
        blockRenderer3D.start(this);
    },

    renderWorld() {
        const canvas = this.elements.worldCanvas || document.getElementById("worldCanvas");
        if (!canvas) {
            renderWorldBefore3D();
            return;
        }
        this.elements.worldMap.classList.add("world-map-3d");
        this.renderPlayerPosition();
        this.updateInteractPrompt();
        blockRenderer3D.start(this);
    },

    renderPlayerPosition() {
        if (this.elements.worldCanvas) {
            blockRenderer3D.requestRender(this);
            return;
        }
        renderPlayerPositionBefore3D();
    },

    movePlayer(direction) {
        if (this.currentScreen !== "worldScreen" || this.isOverlayOpen() || this.isTransitioning) return;

        if (direction === "left" || direction === "right") {
            this.rotateFacing(direction);
            this.updateInteractPrompt();
            this.saveProgress();
            blockRenderer3D.requestRender(this);
            return;
        }

        const moveDirection = direction === "down" ? this.getOppositeFacing(this.facing) : this.facing;
        movePlayerBefore3D(moveDirection);
        blockRenderer3D.requestRender(this);
    },

    updateHud() {
        updateHudBefore3D();
        this.applyStageTheme();
        if (this.elements.playerNameBadge) {
            this.elements.playerNameBadge.textContent = `플레이어: ${this.playerName || "손님"}`;
        }
        if (this.elements.worldViewLabel) {
            const stage = this.getCurrentStage();
            this.elements.worldViewLabel.textContent = `1인칭 과학의 마을 · ${stage.name}`;
        }
    },

    getStageTheme() {
        const stage = this.getCurrentStage?.();
        return stageThemes[stage?.id] || stageThemes[1];
    },

    applyStageTheme() {
        const stage = this.getCurrentStage?.();
        const theme = this.getStageTheme();
        if (!stage || !theme) return;

        document.body.classList.remove("stage-theme-1", "stage-theme-2", "stage-theme-3", "stage-theme-4", "stage-theme-5", "stage-theme-6");
        document.body.classList.add(`stage-theme-${stage.id}`);
        document.documentElement.style.setProperty("--stage-page-bg", theme.pageBackground);
        document.documentElement.style.setProperty("--stage-accent", theme.accent);
        document.documentElement.style.setProperty("--stage-vignette", String(theme.vignette ?? 0.28));
    },

    applySavedGame(savedGame) {
        applySavedGameBefore3D(savedGame);
        this.playerName = this.normalizePlayerName(savedGame.playerName || this.playerName);
        this.facing = ["up", "right", "down", "left"].includes(savedGame.facing) ? savedGame.facing : "right";
        this.lookYaw = Number.isFinite(savedGame.lookYaw) ? savedGame.lookYaw : this.yawFromFacing(this.facing);
        this.lookPitch = Number.isFinite(savedGame.lookPitch) ? this.clampPitch(savedGame.lookPitch) : 0;
    },

    saveProgress(finished = false) {
        try {
            localStorage.setItem(this.storageKey, JSON.stringify({
                stageIndex: this.stageIndex,
                score: this.score,
                emeralds: this.emeralds,
                totalAnswers: this.totalAnswers,
                correctAnswers: this.correctAnswers,
                completedStages: this.completedStages,
                earnedPowers: this.earnedPowers,
                stageWorldState: this.stageWorldState,
                playerName: this.playerName,
                facing: this.facing,
                lookYaw: this.lookYaw,
                lookPitch: this.lookPitch,
                finished
            }));
            this.elements.continueButton.hidden = false;
        } catch (error) {
            // 저장을 사용할 수 없는 브라우저에서도 게임은 계속 진행됩니다.
        }
    },

    normalizePlayerName(value) {
        const trimmed = String(value || "").trim().replace(/[<>]/g, "");
        return trimmed ? trimmed.slice(0, 10) : "손님";
    },

    rotateFacing(direction) {
        const order = ["up", "right", "down", "left"];
        const current = Math.max(0, order.indexOf(this.facing));
        const offset = direction === "right" ? 1 : -1;
        this.facing = order[(current + offset + order.length) % order.length];
    },

    getOppositeFacing(facing) {
        return {
            up: "down",
            right: "left",
            down: "up",
            left: "right"
        }[facing] || "left";
    },

    startStageMusic() {
        const stage = this.getCurrentStage?.();
        const theme = this.getStageTheme?.();
        const music = theme?.music;
        if (!stage || !music) {
            this.stopStageMusic();
            return;
        }
        if (this.musicTimer && this.musicStageId === stage.id) return;
        this.stopStageMusic();

        try {
            const AudioContextClass = window.AudioContext || window.webkitAudioContext;
            if (!AudioContextClass) return;
            this.musicContext = this.musicContext || new AudioContextClass();
            this.musicContext.resume?.();
            this.musicStageId = stage.id;
            let index = 0;
            const playNote = () => {
                if (this.musicStageId !== stage.id) return;
                const now = this.musicContext.currentTime;
                const note = music.notes[index % music.notes.length];
                const bass = music.bass[Math.floor(index / 2) % music.bass.length];
                this.playStageTone(note, 0.52, music.wave, music.volume);
                if (index % 2 === 0) this.playStageTone(bass, 1.35, "sine", music.bassVolume);
                if (index % 4 === 0) {
                    this.playStageTone(bass * 1.5, 1.75, "triangle", music.bassVolume * 0.55);
                    this.playStageTone(bass * 2, 1.75, "triangle", music.bassVolume * 0.42);
                }
                index += 1;
            };
            playNote();
            this.musicTimer = setInterval(playNote, music.interval);
        } catch (error) {
            this.musicTimer = null;
            this.musicStageId = null;
        }
    },

    playStageTone(frequency, duration, type, volume) {
        if (!this.musicContext || !frequency) return;
        const now = this.musicContext.currentTime;
        const oscillator = this.musicContext.createOscillator();
        const gain = this.musicContext.createGain();
        const filter = this.musicContext.createBiquadFilter();
        oscillator.type = type;
        oscillator.frequency.setValueAtTime(frequency, now);
        filter.type = "lowpass";
        filter.frequency.setValueAtTime(type === "sawtooth" ? 520 : 1400, now);
        gain.gain.setValueAtTime(0.0001, now);
        gain.gain.exponentialRampToValueAtTime(Math.max(0.0002, volume), now + 0.04);
        gain.gain.exponentialRampToValueAtTime(0.0001, now + duration);
        oscillator.connect(filter);
        filter.connect(gain);
        gain.connect(this.musicContext.destination);
        oscillator.start(now);
        oscillator.stop(now + duration + 0.05);
    },

    stopStageMusic() {
        if (this.musicTimer) {
            clearInterval(this.musicTimer);
            this.musicTimer = null;
        }
        this.musicStageId = null;
    }
});

const initBeforePhysics = game.init.bind(game);
const resetRuntimeBeforePhysics = game.resetRuntimeState.bind(game);
const enterStageBeforePhysics = game.enterStage.bind(game);
const applySavedGameBeforePhysics = game.applySavedGame.bind(game);

Object.assign(game, {
    inputState: { up: false, down: false, left: false, right: false, sprint: false },
    lookYaw: 0,
    lookPitch: 0,
    mouseLookSensitivity: 0.0038,
    physics: {
        x: 2,
        y: 7,
        vx: 0,
        vy: 0,
        lastTime: 0,
        raf: null
    },

    init() {
        initBeforePhysics();
        document.addEventListener("keyup", (event) => this.handleKeyup(event));
        this.setupMouseLook();
        this.syncPhysicsPosition();
        this.startPhysicsLoop();
    },

    resetRuntimeState() {
        resetRuntimeBeforePhysics();
        const raf = this.physics?.raf || null;
        const lastTime = this.physics?.lastTime || 0;
        this.inputState = { up: false, down: false, left: false, right: false, sprint: false };
        this.lookYaw = this.yawFromFacing(this.facing || "right");
        this.lookPitch = 0;
        this.physics = { x: this.player.x, y: this.player.y, vx: 0, vy: 0, lastTime, raf };
    },

    enterStage(index, resumed = false) {
        enterStageBeforePhysics(index, resumed);
        this.syncPhysicsPosition();
    },

    applySavedGame(savedGame) {
        applySavedGameBeforePhysics(savedGame);
        this.lookYaw = Number.isFinite(savedGame.lookYaw) ? savedGame.lookYaw : this.yawFromFacing(this.facing || "right");
        this.lookPitch = Number.isFinite(savedGame.lookPitch) ? this.clampPitch(savedGame.lookPitch) : 0;
        this.syncFacingFromYaw();
        this.syncPhysicsPosition();
    },

    setupMouseLook() {
        const target = this.elements.worldCanvas || this.elements.worldMap;
        if (!target || target.dataset.mouseLookReady === "true") return;

        target.dataset.mouseLookReady = "true";
        target.addEventListener("click", () => {
            if (this.currentScreen !== "worldScreen" || this.isOverlayOpen()) return;
            target.focus?.();
            target.requestPointerLock?.();
        });

        target.addEventListener("mousedown", (event) => {
            if (event.button !== 0 || this.currentScreen !== "worldScreen" || this.isOverlayOpen()) return;
            if (document.pointerLockElement === target) {
                event.preventDefault();
                this.useTool();
            } else {
                target.focus?.();
                target.requestPointerLock?.();
            }
        });

        target.addEventListener("mousemove", (event) => this.handleMouseLook(event));
        document.addEventListener("mousemove", (event) => this.handleMouseLook(event));
    },

    handleMouseLook(event) {
        if (this.currentScreen !== "worldScreen" || this.isOverlayOpen() || this.isTransitioning) return;

        const target = this.elements.worldCanvas || this.elements.worldMap;
        const pointerLocked = document.pointerLockElement === target;
        const draggingWorld = event.buttons === 1 && (event.target === target || this.elements.worldMap?.contains(event.target));
        if (!pointerLocked && !draggingWorld) return;

        this.lookYaw = this.normalizeYaw(this.lookYaw + event.movementX * this.mouseLookSensitivity);
        this.lookPitch = this.clampPitch(this.lookPitch - event.movementY * this.mouseLookSensitivity);
        this.syncFacingFromYaw();
        blockRenderer3D.requestRender(this);
    },

    handleKeydown(event) {
        const key = event.key.toLowerCase();

        if (!this.elements.dialogueOverlay.hidden && ["e", "enter", " "].includes(key)) {
            event.preventDefault();
            this.closeDialogue();
            return;
        }

        if (!this.elements.rewardModal.hidden && ["enter", " "].includes(key)) {
            event.preventDefault();
            this.closeReward();
            return;
        }

        if (this.currentScreen === "dungeonScreen" && key === "enter") {
            event.preventDefault();
            this.submitAnswer();
            return;
        }

        if (this.currentScreen !== "worldScreen" || this.isOverlayOpen()) {
            return;
        }

        const movementKeys = {
            arrowup: "up",
            w: "up",
            arrowdown: "down",
            s: "down",
            arrowleft: "left",
            a: "left",
            arrowright: "right",
            d: "right"
        };

        if (key === "shift") {
            event.preventDefault();
            this.inputState.sprint = true;
            this.startPhysicsLoop();
            return;
        }

        if (key === " ") {
            event.preventDefault();
            this.useTool();
            return;
        }

        if (movementKeys[key]) {
            event.preventDefault();
            const direction = movementKeys[key];
            if (!this.inputState[direction]) {
                this.applyPhysicsImpulse(direction, 0.42);
            }
            this.inputState[direction] = true;
            this.startPhysicsLoop();
        } else if (key === "e") {
            event.preventDefault();
            this.useMiningTool();
        } else if (key === "q") {
            event.preventDefault();
            this.interact();
        }
    },

    handleKeyup(event) {
        const key = event.key.toLowerCase();
        const movementKeys = {
            arrowup: "up",
            w: "up",
            arrowdown: "down",
            s: "down",
            arrowleft: "left",
            a: "left",
            arrowright: "right",
            d: "right"
        };

        if (key === "shift") {
            event.preventDefault();
            this.inputState.sprint = false;
            return;
        }

        if (movementKeys[key]) {
            event.preventDefault();
            this.inputState[movementKeys[key]] = false;
        }
    },

    startPhysicsLoop() {
        if (this.physics.raf) return;
        const physicsInterval = 1000 / 30;
        let lastPhysicsFrame = 0;
        const step = (time) => {
            const elapsed = time - lastPhysicsFrame;
            if (elapsed >= physicsInterval) {
                lastPhysicsFrame = time - (elapsed % physicsInterval);
                const previousTime = this.physics.lastTime || time;
                const deltaSeconds = Math.min(0.05, (time - previousTime) / 1000);
                this.physics.lastTime = time;
                this.updatePhysics(deltaSeconds);
            }
            this.physics.raf = requestAnimationFrame(step);
        };
        this.physics.raf = requestAnimationFrame(step);
    },

    updatePhysics(deltaSeconds) {
        if (!deltaSeconds) return;

        if (this.currentScreen !== "worldScreen" || this.isOverlayOpen() || this.isTransitioning) {
            this.physics.vx = 0;
            this.physics.vy = 0;
            return;
        }

        const input = this.getInputVector();
        const acceleration = input.sprint ? 15.5 : 11.5;
        const friction = input.active ? 1.8 : 3.2;
        const maxSpeed = input.sprint ? 5.4 : 3.85;

        if (input.active) {
            this.physics.vx += input.x * acceleration * deltaSeconds;
            this.physics.vy += input.y * acceleration * deltaSeconds;
            this.syncFacingFromYaw();
        }

        const drag = Math.max(0, 1 - friction * deltaSeconds);
        this.physics.vx *= drag;
        this.physics.vy *= drag;

        const speed = Math.hypot(this.physics.vx, this.physics.vy);
        if (speed > maxSpeed) {
            const scale = maxSpeed / speed;
            this.physics.vx *= scale;
            this.physics.vy *= scale;
        } else if (!input.active && speed < 0.035) {
            this.physics.vx = 0;
            this.physics.vy = 0;
        }

        if (!this.physics.vx && !this.physics.vy) return;

        const previousTile = { ...this.player };
        const nextX = this.physics.x + this.physics.vx * deltaSeconds;
        const nextY = this.physics.y + this.physics.vy * deltaSeconds;

        if (this.canOccupyPhysicsPosition(nextX, this.physics.y)) {
            this.physics.x = nextX;
        } else {
            this.physics.vx = 0;
        }

        if (this.canOccupyPhysicsPosition(this.physics.x, nextY)) {
            this.physics.y = nextY;
        } else {
            this.physics.vy = 0;
        }

        this.syncPlayerTileFromPhysics(previousTile);
    },

    getInputVector() {
        const forward = this.getLookVector();
        const right = this.getRightVector();
        const vector = { x: 0, y: 0 };
        if (this.inputState.up) {
            vector.x += forward.x;
            vector.y += forward.y;
        }
        if (this.inputState.down) {
            vector.x -= forward.x;
            vector.y -= forward.y;
        }
        if (this.inputState.left) {
            vector.x -= right.x;
            vector.y -= right.y;
        }
        if (this.inputState.right) {
            vector.x += right.x;
            vector.y += right.y;
        }

        const length = Math.hypot(vector.x, vector.y);
        if (!length) return { x: 0, y: 0, active: false, sprint: Boolean(this.inputState.sprint) };
        return { x: vector.x / length, y: vector.y / length, active: true, sprint: Boolean(this.inputState.sprint) };
    },

    movePlayer(direction) {
        if (this.currentScreen !== "worldScreen" || this.isOverlayOpen() || this.isTransitioning) return;

        const vector = this.directionToVector(direction);
        if (!vector) return;

        this.applyPhysicsImpulse(direction, 1.45);
        this.startPhysicsLoop();
    },

    applyPhysicsImpulse(direction, amount) {
        const vector = this.directionToVector(direction);
        if (!vector) return;

        this.syncFacingFromYaw();
        this.physics.vx += vector.x * amount;
        this.physics.vy += vector.y * amount;
    },

    useTool() {
        if (this.currentScreen !== "worldScreen" || this.isOverlayOpen() || this.isTransitioning) return;

        const target = this.getToolTarget();
        if (!target) {
            this.showTemporaryPrompt("앞쪽에 사용할 대상이 없어요.");
            return;
        }

        if (target.kind === "mineTile") {
            this.mineTile(target.x, target.y, target.tile);
            return;
        }

        if (target.kind === "collectible") {
            this.collectItem(target);
            return;
        }

        const handlers = {
            npc: () => this.handleNpc(target),
            sign: () => this.handleSign(target),
            monster: () => this.handleMonster(target),
            pollution: () => this.handlePollution(target),
            bridgeButton: () => this.handleBridgeButton(target),
            sequenceButton: () => this.handleSequenceButton(target),
            portal: () => this.handlePortal()
        };
        handlers[target.kind]?.();
    },

    useMiningTool() {
        if (this.currentScreen !== "worldScreen" || this.isOverlayOpen() || this.isTransitioning) return;

        const target = this.getMineTarget();
        if (!target) {
            this.showTemporaryPrompt("캘 수 있는 나무나 돌이 앞에 없어요.");
            return;
        }

        this.mineTile(target.x, target.y, target.tile);
    },

    getMineTarget() {
        const look = this.getLookVector();
        const origin = this.getPlayerRenderPosition();

        for (let distance = 0.45; distance <= 2.55; distance += 0.2) {
            const x = Math.floor(origin.x + look.x * distance + 0.5);
            const y = Math.floor(origin.y + look.y * distance + 0.5);
            const tile = this.getTileAt(x, y);
            if (this.canMineTile(x, y, tile)) {
                return { kind: "mineTile", x, y, tile };
            }
        }

        return this.getNearbyMineableTile();
    },

    getToolTarget() {
        const look = this.getLookVector();
        const origin = this.getPlayerRenderPosition();
        const visibleEntities = this.getVisibleEntities();

        for (let distance = 0.55; distance <= 2.35; distance += 0.25) {
            const x = Math.floor(origin.x + look.x * distance + 0.5);
            const y = Math.floor(origin.y + look.y * distance + 0.5);
            const entity = visibleEntities.find((candidate) => (
                candidate.x === x && candidate.y === y
                && ["npc", "sign", "monster", "pollution", "bridgeButton", "sequenceButton", "portal", "collectible"].includes(candidate.kind)
            ));
            if (entity) return entity;

            const tile = this.getTileAt(x, y);
            if (this.canMineTile(x, y, tile)) {
                return { kind: "mineTile", x, y, tile };
            }
        }

        return this.getNearbyMineableTile() || this.nearbyEntity || null;
    },

    getNearbyMineableTile() {
        const origin = this.getPlayerRenderPosition();
        const centerX = Math.floor(origin.x + 0.5);
        const centerY = Math.floor(origin.y + 0.5);
        const candidates = [
            { x: centerX, y: centerY - 1 },
            { x: centerX + 1, y: centerY },
            { x: centerX, y: centerY + 1 },
            { x: centerX - 1, y: centerY }
        ];

        for (const candidate of candidates) {
            const tile = this.getTileAt(candidate.x, candidate.y);
            if (this.canMineTile(candidate.x, candidate.y, tile)) {
                return { kind: "mineTile", x: candidate.x, y: candidate.y, tile };
            }
        }

        return null;
    },

    collectItem(item) {
        const state = this.getStageState();
        if (state.collectedIds.includes(item.id)) return;

        state.collectedIds.push(item.id);
        if (item.type === "fragment") {
            state.fragments += item.amount || 1;
            this.score += 15;
            this.showTemporaryPrompt("과학 조각을 획득했어요!");
        } else if (item.type === "key") {
            state.keys += item.amount || 1;
            this.showTemporaryPrompt("포탈 열쇠를 획득했어요!");
        } else {
            this.emeralds += item.amount || 1;
            this.score += 10;
            this.showTemporaryPrompt(`에메랄드 ${item.amount || 1}개 획득!`);
        }

        this.updateHud();
        this.updateQuest();
        this.checkPortalActivation();
        this.renderWorld();
        this.saveProgress();
    },

    mineTile(x, y, tile) {
        const state = this.getStageState();
        const id = this.getTileId(x, y);
        if (state.minedIds.includes(id)) return;

        state.minedIds.push(id);
        this.score += tile === "T" ? 4 : 8;
        if (tile === "R") this.emeralds += 1;
        this.showTemporaryPrompt(tile === "T" ? "나무를 베어 길을 만들었어요!" : "돌을 캐서 에메랄드 1개를 얻었어요!");
        this.updateHud();
        this.renderWorld();
        this.saveProgress();
    },

    getTileAt(x, y) {
        if (x < 0 || y < 0 || x >= this.cols || y >= this.rows) return "";
        return this.getCurrentStage().map[y]?.[x] || "G";
    },

    getTileId(x, y) {
        return `${this.getCurrentStage().id}:${x},${y}`;
    },

    canMineTile(x, y, tile = this.getTileAt(x, y)) {
        if (x <= 0 || y <= 0 || x >= this.cols - 1 || y >= this.rows - 1) return false;
        if (!["R", "T"].includes(tile)) return false;
        return !this.isMinedTile(x, y);
    },

    isMinedTile(x, y) {
        const state = this.getStageState?.();
        return Boolean(state?.minedIds?.includes(this.getTileId(x, y)));
    },

    directionToVector(direction) {
        const forward = this.getLookVector();
        const right = this.getRightVector();
        return {
            up: forward,
            down: { x: -forward.x, y: -forward.y },
            left: { x: -right.x, y: -right.y },
            right
        }[direction] || null;
    },

    facingFromVector(x, y) {
        if (Math.abs(x) > Math.abs(y)) return x > 0 ? "right" : "left";
        return y > 0 ? "down" : "up";
    },

    getLookVector() {
        return {
            x: Math.cos(this.lookYaw || 0),
            y: Math.sin(this.lookYaw || 0)
        };
    },

    getLookVector3D() {
        const pitch = this.lookPitch || 0;
        const horizontal = Math.cos(pitch);
        return {
            x: Math.cos(this.lookYaw || 0) * horizontal,
            y: Math.sin(pitch),
            z: Math.sin(this.lookYaw || 0) * horizontal
        };
    },

    getRightVector() {
        const forward = this.getLookVector();
        return {
            x: -forward.y,
            y: forward.x
        };
    },

    yawFromFacing(facing) {
        return {
            right: 0,
            down: Math.PI / 2,
            left: Math.PI,
            up: -Math.PI / 2
        }[facing] ?? 0;
    },

    normalizeYaw(yaw) {
        const fullTurn = Math.PI * 2;
        return ((yaw + Math.PI) % fullTurn + fullTurn) % fullTurn - Math.PI;
    },

    clampPitch(pitch) {
        return Math.max(-0.72, Math.min(0.72, pitch));
    },

    syncFacingFromYaw() {
        const forward = this.getLookVector();
        this.facing = this.facingFromVector(forward.x, forward.y);
    },

    canOccupyPhysicsPosition(x, y) {
        const radius = 0.28;
        const samples = [
            [x - radius, y - radius],
            [x + radius, y - radius],
            [x - radius, y + radius],
            [x + radius, y + radius],
            [x, y]
        ];

        return samples.every(([sampleX, sampleY]) => {
            const tileX = Math.floor(sampleX + 0.5);
            const tileY = Math.floor(sampleY + 0.5);
            return !this.isBlocked(tileX, tileY);
        });
    },

    syncPlayerTileFromPhysics(previousTile) {
        const nextTile = {
            x: Math.floor(this.physics.x + 0.5),
            y: Math.floor(this.physics.y + 0.5)
        };

        if (nextTile.x === this.player.x && nextTile.y === this.player.y) {
            return;
        }

        this.player = nextTile;
        this.checkCollectibles();
        this.checkDangerZone(previousTile);

        if (this.player.x !== nextTile.x || this.player.y !== nextTile.y) {
            this.syncPhysicsPosition();
            this.physics.vx = 0;
            this.physics.vy = 0;
        }

        this.updateInteractPrompt();
        this.checkPortalEntry();
        this.saveProgress();
    },

    syncPhysicsPosition() {
        if (!this.physics) {
            this.physics = { x: this.player.x, y: this.player.y, vx: 0, vy: 0, lastTime: 0, raf: null };
            return;
        }

        this.physics.x = this.player.x;
        this.physics.y = this.player.y;
        this.physics.vx = 0;
        this.physics.vy = 0;
    },

    getPlayerRenderPosition() {
        return {
            x: this.physics?.x ?? this.player.x,
            y: this.physics?.y ?? this.player.y
        };
    },

    renderMiniMap() {
        const canvas = this.elements.miniMapCanvas;
        if (!canvas || this.currentScreen !== "worldScreen") return;

        const ctx = canvas.getContext("2d");
        const size = canvas.width;
        const radius = 15;
        const cells = radius * 2 + 1;
        const cell = size / cells;
        const stage = this.getCurrentStage();
        const state = this.getStageState();
        const playerPosition = this.getPlayerRenderPosition();
        const centerX = Math.round(playerPosition.x);
        const centerY = Math.round(playerPosition.y);

        ctx.clearRect(0, 0, size, size);
        ctx.fillStyle = "#173218";
        ctx.fillRect(0, 0, size, size);
        ctx.imageSmoothingEnabled = false;

        for (let mapY = centerY - radius; mapY <= centerY + radius; mapY += 1) {
            for (let mapX = centerX - radius; mapX <= centerX + radius; mapX += 1) {
                const screenX = (mapX - centerX + radius) * cell;
                const screenY = (mapY - centerY + radius) * cell;
                let tile = " ";
                if (mapX >= 0 && mapY >= 0 && mapX < this.cols && mapY < this.rows) {
                    tile = stage.map[mapY]?.[mapX] || "G";
                }
                if (this.isMinedTile?.(mapX, mapY) && ["T", "R"].includes(tile)) tile = "G";
                ctx.fillStyle = this.getMiniMapTileColor(tile, mapX, mapY);
                ctx.fillRect(screenX, screenY, Math.ceil(cell), Math.ceil(cell));
            }
        }

        this.getVisibleEntities().forEach((entity) => {
            if (Math.abs(entity.x - centerX) > radius || Math.abs(entity.y - centerY) > radius) return;
            const screenX = (entity.x - centerX + radius + 0.5) * cell;
            const screenY = (entity.y - centerY + radius + 0.5) * cell;
            const dotSize = entity.kind === "portal" ? 5 : 4;
            ctx.fillStyle = this.getMiniMapEntityColor(entity, state);
            ctx.fillRect(screenX - dotSize / 2, screenY - dotSize / 2, dotSize, dotSize);
        });

        const cx = size / 2;
        const cy = size / 2;
        const look = this.getLookVector();
        ctx.save();
        ctx.translate(cx, cy);
        ctx.rotate(Math.atan2(look.y, look.x) + Math.PI / 2);
        ctx.fillStyle = "#ffffff";
        ctx.strokeStyle = "#111111";
        ctx.lineWidth = 2;
        ctx.beginPath();
        ctx.moveTo(0, -8);
        ctx.lineTo(6, 7);
        ctx.lineTo(-6, 7);
        ctx.closePath();
        ctx.fill();
        ctx.stroke();
        ctx.restore();

        ctx.strokeStyle = "rgba(255,255,255,0.52)";
        ctx.lineWidth = 2;
        ctx.strokeRect(1, 1, size - 2, size - 2);

        if (this.elements.miniMapCoords) {
            this.elements.miniMapCoords.textContent = `X ${centerX} · Y ${centerY}`;
        }
    },

    getMiniMapTileColor(tile, x, y) {
        const clean = this.completedStages.length;
        if (tile === " " || x < 0 || y < 0 || x >= this.cols || y >= this.rows) return "#111111";
        if (tile === "C" && clean > ((x + y) % 6)) return "#5fad49";
        return {
            G: "#4f9a3e",
            P: "#b88a54",
            T: "#1f6b2d",
            R: "#8b9092",
            W: "#2e9fc9",
            C: "#6c3d7d",
            F: "#66bd4d",
            D: "#5b4b3f"
        }[tile] || "#4f9a3e";
    },

    getMiniMapEntityColor(entity) {
        const classes = entity.className || "";
        if (entity.kind === "portal") return classes.includes("locked") ? "#9a96a3" : "#c278ff";
        if (entity.kind === "npc") return classes.includes("entity-golem") ? "#e0ddd0" : "#ffcc99";
        if (entity.kind === "monster") return classes.includes("entity-base") ? "#54d6df" : classes.includes("entity-boss") ? "#e05243" : "#67e36f";
        if (entity.kind === "collectible") return classes.includes("entity-emerald") ? "#23ff9a" : classes.includes("entity-key") ? "#f2d34c" : "#74e0ff";
        if (entity.kind === "sign") return "#f0bd75";
        if (entity.kind === "pollution") return "#d05cff";
        if (entity.kind === "bridgeButton" || entity.kind === "sequenceButton") return "#f2c94c";
        if (entity.kind === "warning") return "#ffdf4a";
        return "#fff7cf";
    }
});

document.addEventListener("DOMContentLoaded", () => {
    game.init();
});

window.game = game;
