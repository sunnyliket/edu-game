# 변경 이력

## 2026-06-26
- **목표**: 몬스터 외형 개선 및 흡입 중 접촉 피해 없이 능력 획득하도록 수정
- **수정 사항**:
  - `game.js`: 몬스터를 네모+아이콘 대신 뿔, 가시, 눈, 이빨, 발이 있는 몬스터형 실루엣으로 변경
  - `game.js`: 흡입 중 몬스터가 닿으면 생명이 줄지 않고 해당 몬스터 타입 능력을 즉시 획득하도록 `absorbMonster()` 추가
- **테스트 결과**:
  - `game.js` 문법 파싱 확인 통과
  - 모의 캔버스 환경에서 몬스터 렌더링 완료 확인
  - 흡입 중 몬스터 접촉 시 생명 유지, 능력 획득, 몬스터 비활성화 확인
- **주의사항**:
  - 인앱 브라우저의 로컬 URL 차단 정책 때문에 실제 브라우저 플레이 검증은 수행하지 못함

## 2026-06-26
- **목표**: 몬스터 사망 문구 변경 및 보스 클리어 단축키로 봉인막도 열리게 수정
- **수정 사항**:
  - `game.js`: 몬스터에게 죽었을 때 게임오버 문구를 `조금 더 민첩해집시오..`로 변경
  - `game.js`: 보스 상태에 `barrierOpen`을 추가하고 `Shift+G` 단축키 실행 시 봉인막 표시와 충돌 차단을 함께 해제
- **테스트 결과**:
  - `game.js` 문법 파싱 확인 통과
  - `Shift+G` 실행 후 `boss.barrierOpen=true`, `boss.active=false`, 봉인막 충돌 미적용 확인
  - 몬스터 사망 문구가 `조금 더 민첩해집시오..`로 표시되는 것 확인
- **주의사항**:
  - 인앱 브라우저의 로컬 URL 차단 정책 때문에 실제 브라우저 플레이 검증은 수행하지 못함

## 2026-06-24
- **목표**: 어려움 미로 단계가 사실상 클리어 불가능한 문제 수정
- **수정 사항**:
  - `game.js`: 어려움 미로 벽 간격과 통과 높이를 조정하여 비행으로 지나갈 수 있는 경로 확보
  - `game.js`: 어려움 점프맵 발판 폭을 30px에서 50~55px로 넓혀 연속 가시 구간을 통과 가능하게 조정
- **테스트 결과**:
  - `game.js` 문법 파싱 확인 통과
  - 어려움 미로 샘플 통과 지점들이 벽과 겹치지 않는 것 확인
  - 모의 캔버스 환경에서 어려움 단계 렌더링 완료 확인
- **주의사항**:
  - 인앱 브라우저의 로컬 URL 차단 정책 때문에 실제 브라우저 플레이 검증은 수행하지 못함

## 2026-06-24
- **목표**: 캐릭터와 NPC가 보이지 않는 렌더링 문제 수정 및 `Shift+G` 보스전 자동 클리어 추가
- **수정 사항**:
  - `game.js`: `stroke()` 오타를 `ctx.stroke()`로 수정하여 렌더링 루프 중단 방지
  - `game.js`: `roundRect`/`ellipse` 호환 헬퍼를 추가하여 일부 브라우저에서 캐릭터/NPC 렌더링이 끊기지 않도록 수정
  - `game.js`: `Shift+G` 입력 시 보스 퀴즈를 닫고 보스 클리어 연출로 진입하는 단축키 추가
- **테스트 결과**:
  - `game.js` 문법 파싱 확인 통과
  - `roundRect`/`ellipse`가 없는 모의 캔버스 환경에서 `resetGame()` 및 `draw()` 완료 확인
  - 보스 퀴즈 중 단축키 실행 시 `boss.cinematic=true`, `boss.active=false`, `quiz.active=false` 확인
- **주의사항**:
  - 인앱 브라우저의 로컬 URL 차단 정책 때문에 실제 브라우저 플레이 검증은 수행하지 못함

## 2026-06-17
- **목표**: 보스 클리어 후 포탈 안 미로 구간에서 Space 키를 길게 누르면 날 수 있게 변경
- **수정 사항**:
  - `game.js`: `spaceLevel` 전용 Space 홀드 비행 물리, 비행 파티클, 플레이어 날개 효과, 안내 문구 추가
  - `index.html`: 시작 화면 조작 안내에 포탈 안 비행 설명 추가
- **테스트 결과**:
  - `game.js` 문법 파싱 확인 통과
  - 모의 캔버스 환경에서 `spaceLevel` Space 홀드 시 위쪽 속도와 비행 상태 적용 확인
- **주의사항**:
  - 인앱 브라우저가 로컬 URL을 차단하여 실제 브라우저 플레이 검증은 수행하지 못함
